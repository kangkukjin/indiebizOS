# IndieBiz OS - Core
# Python 3.10+

# 서버
# ★fastapi 상한을 걸지 않는다 — 한 번 걸었다가 되돌렸다(2026-07-25).
# 경위: FastAPI 0.137.0 부터 include_router() 가 라우트를 app.routes 로 펼치지 않고
# `_IncludedRouter`(원본 라우터 + 포함 컨텍스트)를 하나 넣어 요청 시점에 해소한다
# (지연 포함). 그래서 len(app.routes) 가 522 → 46 으로 보이는데, 이걸 "라우터가 등록
# 안 됐다 = 새 설치가 빈 서버가 된다"로 오진해 상한을 걸었다.
# **오진이었다**: 앱은 멀쩡히 응답한다(실측 0.137.0 — /projects·/nas/status·
# /portal/manifest·/xray/summary·/scheduler/tasks 가 전부 401/403 = 인증 게이트 정상
# 동작, 404 아님). app.routes 를 평평한 목록으로 읽던 코드는 이 저장소에
# scripts/check_public_routes.py 하나뿐이었고, 그쪽을 두 표현 모두 읽게 고쳤다.
fastapi
uvicorn[standard]
pydantic
python-dotenv
python-multipart
pyyaml
pyjson5  # IBL params 파싱 (unquoted keys, 양쪽 따옴표, trailing comma 등 JSON5 표준 지원)
# IBL MCP 서버 (mcp_server.py stdio + api.py /mcp HTTP 마운트) — .venv 전환(2026-08-10) 때 누락, 2026-08-13 합류.
# ★2.0 은 mcp.server.fastmcp 제거(FastMCP 별도 배포)라 1.x 고정 — 올리려면 mcp_server.py·api.py 마운트 코드 이주 필요.
mcp<2
psutil  # World Pulse 수집기·platform_utils·[sense:host] — .venv 전환 잔재 감사(2026-08-13)에서 합류

# AI 프로바이더 (3개 모두 지원)
anthropic
openai
google-genai

# Nostr / IndieNet
pynostr
websocket-client

# 도구 핵심 패키지
yt-dlp
youtube-transcript-api
PyMuPDF

# 웹 검색·크롤 (근본 IBL 액션 [sense:search]/[sense:crawl]/[sense:search_news]에 필요 —
# 가볍고 안정적이라 core에 포함해 fresh 설치에서 OOTB 동작. 무거운 도구 의존성은 tools로.)
ddgs
beautifulsoup4
feedparser
requests
nest-asyncio
