# IndieBiz OS - Core
# Python 3.10+

# 서버
# ★fastapi 상한 필수 (2026-07-25) — 안 걸면 새 설치가 조용히 빈 서버가 된다.
# FastAPI 0.137.0 부터 app.include_router() 가 라우트를 하나도 등록하지 않는다.
# 예외도 경고도 없다: 37개 라우터가 전부 무시돼 api.app 라우트가 522 → 46 으로 떨어지고
# (/, /health, /ping, /image 만 남음) 백엔드가 사실상 죽은 채로 "정상 기동"한다.
# 버전 무고정이던 탓에 **오늘 새로 설치하면 그 버전이 잡힌다** — 신규 설치·CI·배포 번들이
# 전부 이 상태였다.
#
# 이분 실측(python3.11, requirements-core 단독):
#   0.128.5 → 522 · 0.130.0 → 522 · 0.135.0 → 522 · 0.136.0 → 522   ← 정상
#   0.137.0 →  46 · 0.138.0 →  46 · 0.139.2 →  46 · 0.140.0 →  46   ← 파손
#   (starlette 를 0.52.1 로 내려도 0.139.2 는 46 → 진범은 fastapi 쪽)
#
# 상한을 올리려면 그 버전에서 라우트 수를 실제로 세어 보고 올릴 것.
# scripts/check_public_routes.py 가 이 부류의 상시 가드다(라우트가 비정상적으로 적으면
# 실패). 이 버그를 처음 잡은 것도 그 가드다 — 처음엔 "공개 라우트 1개"로 조용히 통과했다.
fastapi>=0.115,<0.137
uvicorn[standard]
pydantic
python-dotenv
python-multipart
pyyaml
pyjson5  # IBL params 파싱 (unquoted keys, 양쪽 따옴표, trailing comma 등 JSON5 표준 지원)

# AI 프로바이더 (3개 모두 지원)
anthropic
openai
google-genai

# Nostr / IndieNet
pynostr
websocket-client

# 도구 핵심 패키지
yt-dlp
youtube-transcript-api
PyMuPDF

# 웹 검색·크롤 (근본 IBL 액션 [sense:search_ddg]/[sense:crawl]/[sense:search_news]에 필요 —
# 가볍고 안정적이라 core에 포함해 fresh 설치에서 OOTB 동작. 무거운 도구 의존성은 tools로.)
ddgs
beautifulsoup4
feedparser
requests
nest-asyncio
