# IndieBiz OS - Tool Package Dependencies
# 도구 패키지가 사용하는 라이브러리들
# requirements-core.txt와 함께 설치

# 웹/크롤링 — 핵심(ddgs·beautifulsoup4·feedparser·requests·nest-asyncio)은
# requirements-core.txt로 승격됨(CI가 core를 번들해 OOTB 동작). 검색은 표준 ddgs 단일화
# (구 duckduckgo-search 제거 — blog 포함 전 도구가 ddgs 사용).

# 브라우저 자동화 (browser-action) — 무거워 번들 제외, 로드 실패 시 명확한 에러+안내.
# chromium 바이너리는 별도: playwright install chromium
playwright

# 데이터/시각화
matplotlib
plotly
kaleido
numpy

# 투자
finance-datareader
yfinance

# 미디어/이미지
pillow
moviepy
edge-tts
jinja2

# 학술
arxiv

# Gmail 연동
google-api-python-client
google-auth-oauthlib
google-auth-httplib2

# 기타
markdown
python-pptx

# music-player 패키지 — 음악 태그(ID3/FLAC/MP4) 추출
mutagen

# android 패키지 [sense:here] — macOS OS 위치서비스(WiFi 지문, 랩탑용). 다른 OS 무해(마커).
pyobjc-framework-CoreLocation; sys_platform == "darwin"

# === 2026-08-13 .venv 전환 잔재 감사에서 합류 (3.14 홈브루에만 있던 미선언 의존성) ===
# system_essentials [self:sheet]·office_ops — 엑셀 장부 부분 편집
openpyxl
# system_essentials office_ops·data-ops·lecture_workspace — 워드 문서
python-docx
# computer-use 패키지 — 마우스/키보드/화면(pyautogui) + macOS 캡처·OCR(Quartz/AS/Vision)
pyautogui
pyobjc-framework-Quartz; sys_platform == "darwin"
pyobjc-framework-ApplicationServices; sys_platform == "darwin"
pyobjc-framework-Vision; sys_platform == "darwin"
# 개발·회귀 테스트 (test_pipe_currency_failures.py 등)
pytest
# lecture_workspace 슬러그 로마자화 1순위 (없으면 내장 자모 로마자화 폴백) —
# 의존성 커버리지 감사(scripts/check_import_coverage.py, 2026-08-13)에서 합류
Unidecode
# TLS 크롬위장 (real-estate 직방·네이버 / shopping-assistant 크몽·번개장터 /
# location-services 여기어때 등이 직접 import) — 지금까지 yfinance 의 전이 의존으로만
# 깔려 있었다(감사에서 발견). yfinance 가 떨구면 세 패키지가 같이 죽는 구조라 명시 선언.
curl_cffi
