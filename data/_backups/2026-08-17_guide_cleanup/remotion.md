# Remotion 영상 제작 가이드 (은퇴)

**이 가이드는 2026-08-05 은퇴했다.** engines:remotion 액션과 remotion-video 패키지는
`data/packages/not_installed/tools/remotion-video/` 로 물러났다(사용자 판정: 실사용 저조,
영상의 정본 경로는 강의 덱). 동영상 제작은 `video_workflow.md` — `[self:deck]{op: "video"}` 를 보라.
되살리려면 패키지를 installed/ 로 옮기고 어휘·코퍼스·가이드를 재등록해야 한다(생애주기 대칭).
