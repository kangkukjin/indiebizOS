# 폴더 조사 — 포식 기억 만들기 지침 [self:forage]

사용자가 폴더를 지명하면(말로, 또는 PC 관리 창의 "🧠 기억" 판에서) **네가** 그 폴더의 포식 기억을 만든다. 정해진 형식도, 정해진 도구도 없다 — **자료가 형식을 정하고, 어떻게 볼지는 네가 정한다.** 설계 정본: `docs/FOLDER_SURVEY_HANDOFF.md`.

## 무엇을 만드나 — 둘
1. **단언(기억)** — `[self:forage]{op: "note", layer: "map", locus: "<폴더>", kind: "<identity|convention|dead_branch|substrate>", claim: "<한 줄>"}`. 루트 한 건은 `territory: true`. **정본은 문서다(2026-09-03 사용자 판정)**: note 로 적은 단언은 그 위치를 덮는 문서의 `## 단언` 절에 자동으로 그려지고(색인 forage_map 은 그 절의 사본), 문서의 그 절을 편집기·판에서 고치면 `[self:forage]{op: "sync", locus}` 또는 다음 `recall` 이 색인을 맞춘다. 절의 표기: `- [종류] 표식 한 줄 ‹메타›`(⚑영토 ↓하위에도 ≈의미적 ?의심). 문서를 통째로 다시 쓸 때 이 절을 지우면 단언도 지워진다 — 절은 보존하고 그 안의 줄을 고쳐라. `recall{locus}` 결과의 `doc` 이 문서 경로다. 관습·기질이 하위에도 통하면 `generalizes: true`. **내용 요약이 아니라 판단**이 다음 검색을 살린다:
   - 분할의 축(주제인가 시기인가 상태인가) · 겹치는 축과 **우선순위**(같은 것이 두 폴더에 갈 수 있을 때 어디로 가나)
   - 명명 관습 · **예외의 위치**(이름과 다른 것이 어디 있나) · **죽은 가지**(이 폴더에 그것은 없다) · 기질(외장·마운트·포맷·크기)
   - **주제어**(이 폴더를 부를 낱말들 — 회상이 낱말로도 걸리게)
2. **문서(정본·사람이 읽고 고친다)** — `~workspace/data/forage_surveys/<몸>/<경로 그대로>/memory.md` — **폴더 트리를 비춘 트리**라 어느 폴더의 문서인지, 그 아래에 더 자세한 문서가 있는지(`recall` 의 `docs_below`)가 디렉토리에서 바로 드러난다. **이름·자리는 기계가 정한다**(절대 경로가 있는 단언은 `body` 표기가 `code:…` 든 `disk:…` 든 **경로 트리 한 곳**에 놓인다 — 경로는 경로다. URL 도 같다 — `host/path` 로 정규화해 `web/<host>/<path>/memory.md` 트리 한 곳(스킴·`?query` 무관, 처음엔 host+첫 단 급). `code:<저장소>`·`book:` 문서와 `web/memory.md` 뿌리에는 경로·URL 없는 단언(주제 라벨)만 남는다. 첫 `note` 때 상위 폴더 급에 만들어지고, 어느 폴더에 `territory: true` 를 찍으면 그 폴더 자기 노드에 문서가 생긴다 = 조사된 뿌리; `recall{locus}` 결과의 `doc` 이 그 경로). 새 이름을 짓지 말고 그 문서에 산문(정체·축·겹침·예외·계보·찾는 법)을 채워라 — `## 단언` 절은 기계가 그리니 건드리지 말고, 다른 절을 쓴다. 머리에 **조사 일시 · 어디까지 봤나(예산) · 거칠기 · 상위/하위 문서 링크**를 적어 다음 AI 가 "아직 거칠다"와 "더 상세한 것이 어디 있나"를 안다. 끝에 `## 갱신 기록`(일시 + 한 줄, append 전용). **사용자가 고친 줄은 선언이다** — 재조사 때 지우지 말고 보존한다.

## 자료에 따라 필요한 것을 넣어라
- 이름이 뜻을 담는 폴더(영상·사진·문서·음악)는 **아이템 목록**이 값을 한다 — 그 자료를 *찾을 때 쓰이는 속성만*(영상: 제목·연도·상태·자막 / 논문: 저자·연도·주제 / 사진: 시기·장소·기기 / 코드: 모듈·언어). 형식은 네가 정한다(표·JSON·시트). 크면 문서 옆 파일로 두고 문서에 경로를 적는다.
- 본문 전문은 RAG 몫이다. 여기엔 **정리의 판단**과 **내용에 없는 속성**만.
- **편수는 파일 수가 아니라 작품 수**다 — CD1/CD2 부속·자막·부록을 합쳐 한 편으로 센다(실측: 파일 49 = 작품 27). 문서의 축 표엔 작품 수를 적고 파일 수는 괄호로.
- **셈 질문("몇 편")은 문서가 먼저 답한다** — 문서의 표에 이미 있으면 그걸로. 없으면 목록 파일을 IBL 파이프로 통째 굴리지 말고(큰 목록은 단계 입력 상한에 걸린다) `run_command` 로 짧은 계수 스크립트를 돌려라.
- 무엇이 검색에 쓰일지는 자료를 보고 정한다 — 이 지침의 예를 형식으로 삼지 말 것.
- **이미 있는 색인을 먼저 찾아 쓴다** — 사진 폴더면 사진 관리 창의 스캔(`data/packages/photo_scans/scans.json` 에 폴더별 스캔·`scan_<id>.db` 의 `media_files`: 촬영일·GPS·기종·해시), 블로그면 검색 색인, 문서 더미면 노트북 소스. 그 폴더를 덮는 색인이 있으면 **목록을 다시 만들지 말고** 단언(`substrate`)에 "이 폴더는 사진 스캔이 있다(경로·시각·장수·날짜 범위)" 처럼 적어 다음 AI 가 그 색인으로 답하게 하라. 색인이 낡았으면(폴더 변경 시각 > 스캔 시각) 그것도 단언에.

## 프로젝트 폴더 (2026-09-05 사용자 판정 "주입 쪽으로")
- `~workspace/projects/<프로젝트>/` 의 포식 문서는 그 프로젝트 에이전트의 안정 프롬프트에 `<project_memory doc="<정본 경로>">` 로 **통째** 실린다 — 코딩 하네스의 CLAUDE.md 자리. 폴더가 하나로 고정이라 고를 판단이 없으므로 자동 주입 금지(09-03) 와 충돌하지 않는다. 조상(`projects/`·저장소 루트)의 ↓ 관습·기질은 블록 끝 "상속 관습" 절로 따라온다.
- 24KB 를 넘으면 잘린다 — 프로젝트 문서는 **골격만**(정체·자리·예외·죽은 가지·찾는 법). 목록·수치는 넣지 말고 산출물은 이름 규칙으로 가리킨다.
- **재조사는 주 1회 트리거** `forage_resurvey_<프로젝트>` (03:30, 요일 분산)가 `[others:delegate]{scope:"cross"}` 로 그 프로젝트 에이전트에게 위임한다. 재조사 = 변화만 반영, 사용자가 고친 줄 보존, `## 갱신 기록` 한 줄. 새 프로젝트를 만들면 문서 첫 조사와 트리거 등록을 같이 한다.
- 턴 중에 새로 안 것은 재조사를 기다리지 말고 그 자리에서 `[self:forage]{op:"note", locus:"<프로젝트 경로>"}` 로 남긴다 — 정기 재조사는 그물이지 주 경로가 아니다.

## 예산 = 해상도 — 기본값이 있다 (2026-09-03 사용자 판정)
| 축척 | 언제 | 도구 호출 | 시간 | 문서 크기 |
|---|---|---|---|---|
| **거칠게(골격)** — 기본 | 첫 조사, 넓은 폴더(수백 폴더·수만 파일) | 20회 안팎 | 5분 안팎 | 6KB 안팎(한글 60~80줄) |
| **자세히(아이템까지)** | 사용자가 그 폴더를 지명하며 "자세히"라 했거나 작은 폴더 | 60회 안팎 | 15분 안팎 | 20KB 안팎(200~280줄) |

- 사용자의 한마디에 "자세히"·"거칠게"가 있으면 그 축척. 없으면 기본(거칠게).
- **예산이 차면 그 자리에서 멈춘다.** 문서 머리에 "여기까지 봤음 — 더 보려면 `<하위 폴더>` 를 지명" 이라고 적는다. 상한이 아니라 기본값이므로 자료를 보고 넘길 수 있되, 넘긴 이유를 머리에 적는다.
- 문서 크기는 **목표가 아니다** — 거칠게 조사한 문서가 6KB 를 채울 필요는 없다. 정리 기준(아래 24KB)은 안전판이지 채울 자리가 아니다.
- 넓은 폴더는 자식 폴더마다 한 줄인 **골격**만. 작은 폴더는 아이템까지. 얼마나 봤는지를 문서 머리에.
- 상위 조사와 하위 조사는 공존한다: 조상의 `generalizes` 단언은 상속되고, 같은 자리는 더 구체적 조사가 이기며, 어긋나면 조상 단언에 `surface_flag: true`.
- 보는 법은 네 몫: `[self:list]` · `[self:file_find]` · `[self:read]`(표본) · `[self:grep]`. 세거나 파싱할 게 많으면 `[self:write]` + `run_command` 로 **네가 짠** 계수 스크립트를 쓰고, 다시 쓸 만하면 `[self:script]{op: "register"}` 로 올린다(결정화 사다리).

## 문서의 위계 — 상위 문서는 하위 문서를 가리킨다
- 문서 머리에 **상위 폴더 문서**(있으면)와 **더 상세한 하위 폴더 문서**의 경로를 적는다: "이 폴더의 `<자식>` 은 따로 조사됨 → `<문서 경로>` (일시·거칠기)". 상위 문서는 그 자식을 한 줄 골격으로만 두고 상세는 하위 문서에 맡긴다(가림 규칙).
- 하위 폴더를 새로 조사했으면 **상위 문서의 링크 목록을 갱신**한다 — 위계는 문서에도 있어야 사람이 따라간다(단언 쪽은 `recall{locus}` 의 child 가 구조적으로 안다).

## 갱신 — 기억 파일의 정리 방식을 따른다 (append → 정리)
심층 메모리·포식 단언은 쓰기 시점 중복 제거(SAME/UPDATE/REPLACE)와 주기 정리 패스(`forage_consolidation`: 근접 중복 병합·가지치기)를 갖는다. **문서도 같은 생명주기**를 따르되, 문서의 정리는 네가 한다.
1. **append** — 새로 안 것은 문서 끝 `## 갱신 기록` 에 **일시와 함께 한 줄씩** 덧붙인다(재조사 차이, 검색 중 만난 예외, 주인의 한마디). 본문을 매번 다시 쓰지 않는다 — 싸고, 무엇이 언제 바뀌었는지 남는다.
2. **정리** — 셋 중 하나면: ①`## 갱신 기록` 이 **20줄** 을 넘거나 ②문서 전체가 **24KB**(약 250~340줄)를 넘거나 ③기록의 한 줄이 본문과 **어긋나면**(실제로는 이것이 먼저 온다) — **본문에 녹여 다시 쓰고 기록은 비운다**. 정리에서 지키는 것: ①**주인이 고친 줄(선언)은 보존** — 관찰이 선언을 덮지 못한다 ②머리(일시·예산·거칠기·위계 링크)를 현재로 ③더 이상 참이 아닌 단언은 지우지 말고 "~까지는 그랬음"으로 남기거나 `forget` 과 짝 맞춘다 ④문서가 길어지는 진짜 이유가 "폴더가 넓다"면 압축이 아니라 **하위 폴더 문서로 분할**하고 상위는 골격+링크만 남긴다(예산=해상도).
3. **단언과 문서를 같이** — 문서를 고쳤으면 그 판단은 `note` 로도, 단언을 `forget` 했으면 문서에서도. 둘이 어긋나면 사람이 읽는 쪽(문서)이 낡았다고 보고 문서를 고친다.
- **폴더가 없어지면 기억은 접힌다(자동)** — 회상이 문서 뿌리의 실제 폴더가 없음을 보면(그리고 주간 정리 패스가 전체를) `reconcile` 이 그 노드(와 밑)의 문서를 `_gone/` 으로 접고 부모 문서 갱신 기록에 한 줄을 남긴다. 단언은 남되 `폴더 사라짐` 표식. `_gone/` 은 일주일 지나면 지워진다. **이사는 찾지 않는다**(사용자 판정 — 다시 조사하는 편이 이사를 찾는 것보다 싸다): 옮긴 폴더는 새 자리에서 다시 조사하라. 내용물만 바뀐 흔한 경우는 낡음 표식뿐, 아무것도 지우지 않는다.
- 재조사 = **차이만**. 문서 머리의 일시·예산과 폴더의 변경 시각을 먼저 대조한다.
- 검색 중 단언과 어긋나는 것을 만나면 그 자리에서 `note`(surface_flag) 또는 `forget` — 그리고 문서 `## 갱신 기록` 에 한 줄.

## 회상
자동 주입은 없다. 폴더·파일·자료 위치 질문이면 **답하기 전에** `[self:forage]{op: "recall", locus: "<폴더>"}` — 그 폴더의 단언·물려받은 관습·자식 골격이 통째로 온다. 폴더를 모르면 `query`.

## 실측 기록 (자동 누적)

> 실행 에이전트가 턴 종료 후 덧붙인다.
- 2026-09-03 실측: `recall{locus}` 로 돌아온 단언이 모두 폴더 조사 산물이라는 보장이 없다 — Desktop 단언 17건은 provenance 가 전부 지난 대화 턴의 사용자 질문 문장이었고(경량 모델 증류 부산물), 폴더를 훑어서 생긴 것은 하나도 없었다.
- 2026-09-03 실측: 단언은 17건 있는데 `data/forage_surveys/` 에 해당 문서는 없을 수 있다 — Desktop 은 단언만 있고 문서가 없어 territory 단언의 '사람이 읽는 사전: <경로>' 도 없었다(문서 폴더에는 `Volumes_Expansion_영화.md` 하나뿐).
- 2026-09-03 실측: 대화 증류 단언은 디스크와 크게 어긋난다 — Desktop 단언 17건 중 7건이 이미 사라진 대상(`Desktop/total`·`Desktop/사진자료모음` 은 2026-08-19 에 `/Volumes/<외장 라벨>/` 로 이사)이라 회수했다.

## 노트북(문서 더미)도 폴더다 — 등록하면 자동 조사 (2026-09-03 사용자 판정)

`[self:notebook]` 의 create/add 는 시스템 AI 에게 **노트북 조사**를 자동 위임한다(10분 디바운스). 조사는 이 가이드를 그대로 따르되 대상이 소스 목록이다.
- 몸 = `notebook:<이름>`, 뿌리 locus = `notebook:<이름>`, 소스별 locus = `notebook:<이름>/<소스 제목>`. `[self:forage]{op:"recall", body:"notebook:<이름>", locus:"notebook:<이름>"}` 의 `doc` 가 문서 자리(`~workspace/data/forage_surveys/notebook_<이름>/memory.md`).
- 무엇을 적나: 노트북 전체의 정체(무슨 물음에 답하는 더미인가, `territory: true`) · 소스마다 정체(문서가 무엇인가, 연도·저자·형식) · **그 소스가 답할 수 있는 물음과 못 하는 물음** · 소스 간 겹침·모순 · 용어(같은 것을 다르게 부르는 말). 산문은 문서에, 단언은 `note` 로.
- 어떻게 보나: `[self:notebook]{op:"sources", name}` 로 목록, 소스는 `[self:read]` 로 앞부분·목차를 읽는다(거칠게 축척 — 소스마다 핵심만, 문서 6KB 안). 스캔 PDF·자막 없는 영상은 "읽을 수 없음"을 단언으로 남긴다.
- 쓰임: `ask` 는 이 문서를 발췌 판단의 지도로 함께 읽고, 어느 소스인지 알면 `source` 로 좁혀 묻는다(`[self:notebook]{op:"ask", name, query, source:"<제목 일부>"}`). 질의로 새로 안 것(오분류·빈 소스·예외)은 되적는다.
