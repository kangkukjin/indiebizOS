# IBL 교재 위치 안내

현재 IBL의 주 교재는 **ibl_composition.md**다. `read_guide(query="ibl_composition.md")`로 읽는다.
문법 전문은 `data/common_prompts/fragments/12_ibl_only.md`, 언어 설계 정본은 `data/system_docs/ibl.md`다.
이 파일은 이전 링크를 위한 안내이며 별도 판본 교재가 아니다. 새 코드는 현재 문법으로 작성한다.
