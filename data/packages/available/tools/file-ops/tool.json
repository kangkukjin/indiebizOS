[
  {
    "name": "read_file",
    "description": "파일의 내용을 읽습니다.",
    "input_schema": {
      "type": "object",
      "properties": {
        "file_path": {
          "type": "string",
          "description": "읽을 파일의 경로"
        }
      },
      "required": ["file_path"]
    }
  },
  {
    "name": "write_file",
    "description": "파일에 내용을 씁니다.",
    "input_schema": {
      "type": "object",
      "properties": {
        "file_path": {
          "type": "string",
          "description": "쓸 파일의 경로"
        },
        "content": {
          "type": "string",
          "description": "파일에 쓸 내용"
        }
      },
      "required": ["file_path", "content"]
    }
  },
  {
    "name": "list_directory",
    "description": "디렉토리의 파일과 폴더 목록을 가져옵니다.",
    "input_schema": {
      "type": "object",
      "properties": {
        "dir_path": {
          "type": "string",
          "description": "디렉토리 경로 (기본: 현재 디렉토리)"
        }
      },
      "required": []
    }
  }
]
