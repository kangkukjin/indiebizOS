[
  {
    "name": "create_slides",
    "description": "텍스트와 이미지를 조합하여 홍보용 슬라이드 이미지들을 생성합니다.\n\n입력 데이터: slides=[{title: '제목', body: '내용', image_path: '이미지경로', bg_color: '#RRGGBB', text_color: '#RRGGBB'}, ...]\n\n반환: 생성된 이미지 경로 리스트",
    "input_schema": {
      "type": "object",
      "properties": {
        "slides": {
          "type": "array",
          "items": {
            "type": "object",
            "properties": {
              "title": {"type": "string", "description": "슬라이드 제목"},
              "body": {"type": "string", "description": "슬라이드 본문 내용"},
              "image_path": {"type": "string", "description": "삽입할 이미지 파일 경로 (선택)"},
              "bg_color": {"type": "string", "description": "배경색 (예: #FFFFFF)"},
              "text_color": {"type": "string", "description": "텍스트 색상 (예: #000000)"},
              "template_id": {
                "type": "string",
                "description": "디자인 템플릿 (modern, tech, minimal 중 선택)",
                "default": "modern"
              }
            },
            "required": ["title", "body"]
          }
        },
        "output_dir": {"type": "string", "description": "결과물을 저장할 디렉토리 경로 (선택)"}
      },
      "required": ["slides"]
    }
  },
  {
    "name": "create_video",
    "description": "이미지 파일들을 합쳐서 전환 효과와 배경음악이 포함된 MP4 홍보 영상을 제작합니다.\n\n입력 데이터: images=['path1.png', 'path2.png', ...], duration_per_slide=3, bgm_path='bgm.mp3'\n\n반환: 생성된 영상 파일 경로",
    "input_schema": {
      "type": "object",
      "properties": {
        "images": {
          "type": "array",
          "items": {"type": "string"},
          "description": "영상으로 합칠 이미지 경로 리스트"
        },
        "duration_per_slide": {
          "type": "number",
          "description": "슬라이드당 재생 시간 (초)",
          "default": 3
        },
        "bgm_path": {
          "type": "string",
          "description": "배경음악 파일 경로 (선택)"
        },
        "narration_texts": {
          "type": "array",
          "items": {"type": "string"},
          "description": "각 슬라이드별 나레이션 텍스트 (선택)"
        },
        "voice": {
          "type": "string",
          "description": "TTS 목소리 (ko-KR-SunHiNeural, ko-KR-InJoonNeural 등)",
          "default": "ko-KR-SunHiNeural"
        },
        "output_filename": {
          "type": "string",
          "description": "저장할 파일 이름 (예: promo.mp4)",
          "default": "promotion_video.mp4"
        }
      },
      "required": ["images"]
    }
  }
]
