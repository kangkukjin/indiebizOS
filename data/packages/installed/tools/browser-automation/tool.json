{
  "name": "browser_action",
  "description": "브라우저 자동화 (Playwright). 자연어로 웹 작업 지시. 복잡한 웹 조작, 로그인 필요 사이트 등에 사용. 단순 검색은 ddgs_search 사용 권장.",
  "input_schema": {
    "type": "object",
    "properties": {
      "instruction": {
        "type": "string",
        "description": "구체적 작업 지시 (예: '네이버에서 환율 검색해서 알려줘')"
      }
    },
    "required": ["instruction"]
  }
}
