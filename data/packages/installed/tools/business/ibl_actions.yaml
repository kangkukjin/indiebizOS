node: others
actions:
  neighbors:
    description: 비즈니스 이웃 조회 통합. neighbor_id/name 있으면 단건 상세, 없으면 목록(검색·필터).
    target_description: 'neighbor_id (단건 상세) 또는 name (정확 일치 단건 상세). 또는 search (부분 일치 검색), info_level (0-4 필터). 모두 생략 시 전체 목록.'
    router: handler
    tool: neighbors_op
    implementation: handler.py가 neighbor_id/name 유무로 분기 — 있으면 get_neighbor_detail, 없으면 get_neighbors.
    target_key: search
