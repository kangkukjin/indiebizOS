{
  "name": "execute_python",
  "description": "Python 코드 실행. 계산, 데이터 처리, 파일 조작용. 한 번 호출로 실행 및 결과 반환. 무한루프/위험코드 자제.",
  "input_schema": {
    "type": "object",
    "properties": {
      "code": { "type": "string", "description": "실행할 Python 코드" }
    },
    "required": ["code"]
  }
}
