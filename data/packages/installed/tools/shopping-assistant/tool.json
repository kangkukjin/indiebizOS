{
  "_generated": "build_ibl_nodes.py가 ibl_actions.yaml의 tool_json 블록에서 파생 — 직접 편집 금지. op-bearing 도구의 op enum/default는 actions의 ops가 단일 소스.",
  "id": "shopping-assistant",
  "name": "상품·중고",
  "description": "새 상품 가격비교와 중고 거래 매물을 검색합니다.",
  "version": "1.3.0",
  "tools": [
    {
      "name": "search_shopping",
      "description": "새 상품 가격비교 검색을 수행합니다. 다나와 가격비교(순수 HTTP — PC·폰 모두 동작). ★중고 매물은 이 도구가 아니라 [sense:used] 를 쓰십시오 — 중고 축(site=used/all)은 2026-08 은퇴했습니다 (사이트 개편으로 스크래핑 0건, 같은 소스를 [sense:used] 가 내부 API 로 제공). ★네이버 축도 2026-08 은퇴 — 공식 API 404 SE05, 내부 API 418 봇차단, site:naver 는 안내만 반환.\n\n지원 사이트: danawa(기본)\n\n데이터 형식: {items: [{name, price, mall, link, image, category, site, spec}], total}",
      "input_schema": {
        "type": "object",
        "properties": {
          "query": {
            "type": "string",
            "description": "검색할 상품명"
          },
          "site": {
            "type": "string",
            "description": "검색할 사이트 — danawa(기본이자 유일한 실동작 축, 가격비교·폰서도 동작). naver 는 은퇴(안내만 반환)",
            "enum": [
              "danawa",
              "naver"
            ],
            "default": "danawa"
          },
          "limit": {
            "type": "integer",
            "description": "검색 결과 개수 (최대 10)",
            "default": 5
          }
        },
        "required": [
          "query"
        ]
      }
    },
    {
      "name": "used_search",
      "description": "중고 C2C 매물 검색. source로 플랫폼 선택 — bunjang(번개장터, 내부 API, 위치 포함·region 후필터·기본), danggeun(당근, region=동이름으로 동네+인근 스코프 — 동네 매물엔 우선), joongna(중고나라 web), naver(네이버 카페 중고 게시글). 통화: {records: [{title, meta, summary, url, image}], total, source}.",
      "input_schema": {
        "type": "object",
        "properties": {
          "query": {
            "type": "string",
            "description": "검색어 (예 \"맥미니 m4 pro\")"
          },
          "source": {
            "type": "string",
            "description": "검색 플랫폼 (동네 매물=danggeun+region, 위치 표기 매물=bunjang+region)",
            "enum": [
              "bunjang",
              "danggeun",
              "joongna",
              "naver"
            ],
            "default": "bunjang"
          },
          "region": {
            "type": "string",
            "description": "지역 필터 — bunjang=위치 substring(예 \"청주\"), danggeun=동이름(예 \"죽백동\", 그 동네+인근 동 스코프)"
          },
          "limit": {
            "type": "integer",
            "description": "결과 개수 (기본 15)",
            "default": 15
          }
        },
        "required": [
          "query"
        ]
      }
    }
  ]
}
