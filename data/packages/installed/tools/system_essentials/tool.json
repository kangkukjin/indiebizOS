{
  "id": "system_essentials",
  "name": "System Essentials Kit",
  "description": "파일/시스템 기본 도구",
  "version": "1.0.0",
  "author": "System AI",
  "tools": [
    {
      "name": "read_file",
      "description": "파일 내용 읽기. 한 번 호출로 전체 내용 반환.",
      "input_schema": {
        "type": "object",
        "properties": {
          "file_path": { "type": "string", "description": "파일 경로" }
        },
        "required": ["file_path"]
      }
    },
    {
      "name": "write_file",
      "description": "파일에 내용 쓰기. 한 번 호출로 완료.",
      "input_schema": {
        "type": "object",
        "properties": {
          "file_path": { "type": "string", "description": "파일 경로" },
          "content": { "type": "string", "description": "기록할 내용" }
        },
        "required": ["file_path", "content"]
      }
    },
    {
      "name": "list_directory",
      "description": "디렉토리 목록 조회. 한 번 호출로 모든 파일/폴더 반환.",
      "input_schema": {
        "type": "object",
        "properties": {
          "dir_path": { "type": "string", "description": "디렉토리 경로 (기본: 현재)" }
        }
      }
    },
    {
      "name": "grep_files",
      "description": "파일 내용 검색. 한 번 호출로 매칭 라인+번호 반환. 정규표현식/glob 지원.",
      "input_schema": {
        "type": "object",
        "properties": {
          "pattern": { "type": "string", "description": "검색 텍스트/정규표현식" },
          "file_pattern": { "type": "string", "description": "파일 패턴 (예: '*.py')" },
          "root_path": { "type": "string", "description": "검색 루트 경로" },
          "regex": { "type": "boolean", "description": "정규표현식 사용 (기본 false)" }
        },
        "required": ["pattern"]
      }
    },
    {
      "name": "get_current_time",
      "description": "현재 날짜/시간 조회. 파라미터 없이 한 번 호출.",
      "input_schema": {
        "type": "object",
        "properties": {
          "format": { "type": "string", "description": "포맷 (기본: %Y-%m-%d %H:%M:%S)" }
        }
      }
    },
    {
      "name": "run_command",
      "description": "쉘 명령어 실행. 위험 명령(rm, sudo)은 approved=true 필요.",
      "input_schema": {
        "type": "object",
        "properties": {
          "command": { "type": "string", "description": "쉘 명령어" },
          "timeout": { "type": "number", "description": "제한시간(초, 기본 60, 최대 300)" },
          "approved": { "type": "boolean", "description": "위험 명령 승인 여부" }
        },
        "required": ["command"]
      }
    },
    {
      "name": "glob_files",
      "description": "glob 패턴으로 파일 검색. 한 번 호출로 매칭 파일 목록 반환.",
      "input_schema": {
        "type": "object",
        "properties": {
          "pattern": { "type": "string", "description": "glob 패턴 (예: '**/*.py')" },
          "root_path": { "type": "string", "description": "검색 루트 경로" }
        },
        "required": ["pattern"]
      }
    },
    {
      "name": "edit_file",
      "description": "파일 부분 수정 (old→new 교체). 전체 재작성 없이 정확한 수정.",
      "input_schema": {
        "type": "object",
        "properties": {
          "file_path": { "type": "string", "description": "파일 경로" },
          "old_string": { "type": "string", "description": "교체할 기존 문자열 (고유해야 함)" },
          "new_string": { "type": "string", "description": "새 문자열" }
        },
        "required": ["file_path", "old_string", "new_string"]
      }
    },
    {
      "name": "open_in_browser",
      "description": "URL/파일을 브라우저에서 열기.",
      "input_schema": {
        "type": "object",
        "properties": {
          "path": { "type": "string", "description": "URL 또는 파일 경로" }
        },
        "required": ["path"]
      }
    },
    {
      "name": "todo_write",
      "description": "작업 목록을 관리합니다. 복잡한 작업을 추적하고 진행 상황을 보여줍니다. 3단계 이상의 작업, 복잡한 작업, 여러 작업이 있을 때 사용하세요.",
      "input_schema": {
        "type": "object",
        "properties": {
          "todos": {
            "type": "array",
            "description": "작업 목록",
            "items": {
              "type": "object",
              "properties": {
                "content": { "type": "string", "description": "작업 내용 (명령형: '테스트 실행')" },
                "status": { "type": "string", "enum": ["pending", "in_progress", "completed"], "description": "상태" },
                "activeForm": { "type": "string", "description": "진행 중 표시 (현재형: '테스트 실행 중')" }
              },
              "required": ["content", "status", "activeForm"]
            }
          }
        },
        "required": ["todos"]
      }
    },
    {
      "name": "ask_user_question",
      "description": "사용자에게 질문하여 선호도, 요구사항을 확인하거나 구현 방향을 결정합니다. 모호한 지시를 명확히 하거나 선택지를 제시할 때 사용하세요.",
      "input_schema": {
        "type": "object",
        "properties": {
          "questions": {
            "type": "array",
            "description": "질문 목록 (1-4개)",
            "items": {
              "type": "object",
              "properties": {
                "question": { "type": "string", "description": "질문 내용" },
                "header": { "type": "string", "description": "짧은 라벨 (최대 12자)" },
                "options": {
                  "type": "array",
                  "description": "선택지 (2-4개)",
                  "items": {
                    "type": "object",
                    "properties": {
                      "label": { "type": "string", "description": "선택지 텍스트" },
                      "description": { "type": "string", "description": "선택지 설명" }
                    },
                    "required": ["label", "description"]
                  }
                },
                "multiSelect": { "type": "boolean", "description": "복수 선택 허용 (기본 false)" }
              },
              "required": ["question", "header", "options"]
            }
          }
        },
        "required": ["questions"]
      }
    },
    {
      "name": "enter_plan_mode",
      "description": "복잡한 구현 작업 전에 계획 모드로 진입합니다. 코드 탐색 후 구현 계획을 수립하고 사용자 승인을 받습니다. 새 기능 구현, 여러 접근 방식이 가능한 경우, 아키텍처 결정이 필요한 경우, 여러 파일 수정이 필요한 경우 사용하세요.",
      "input_schema": {
        "type": "object",
        "properties": {}
      }
    },
    {
      "name": "exit_plan_mode",
      "description": "계획 모드에서 계획 수립을 완료하고 사용자 승인을 요청합니다. 계획 파일에 구현 계획을 작성한 후 사용하세요.",
      "input_schema": {
        "type": "object",
        "properties": {}
      }
    }
  ]
}
