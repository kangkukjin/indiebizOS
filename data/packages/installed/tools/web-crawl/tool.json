{
  "name": "crawl_website",
  "description": "웹페이지 크롤링 (텍스트 추출). 한 번 호출로 URL 내용 반환. 단순 검색은 ddgs_search 사용 권장.",
  "input_schema": {
    "type": "object",
    "properties": {
      "url": { "type": "string", "description": "크롤링할 URL (http/https 포함)" },
      "max_length": { "type": "integer", "description": "최대 텍스트 길이 (기본 10000)", "default": 10000 }
    },
    "required": ["url"]
  }
}
