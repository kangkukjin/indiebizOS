nodes:
  self:
    actions:
      launch:
        description: 즐겨찾기 런처 대시보드 띄우기(기본) 또는 사이트 관리. action 미지정 시 대시보드 UI 표시
        router: handler
        tool: launch_sites
        target_key: action
  sense:
    actions:
      search:
        description: 종합 검색 (웹 + 뉴스 + 유튜브 + 논문 병렬 검색)
        router: handler
        tool: unified_search
        target_key: query
      web_search:
        description: DuckDuckGo 웹 검색
        router: handler
        tool: ddgs_search
        target_key: query
      crawl:
        description: 웹페이지 크롤링 (텍스트 추출)
        router: handler
        tool: crawl_website
        target_key: url
      search_news:
        description: 구글 뉴스 검색
        router: handler
        tool: google_news_search
        target_key: query
    guides:
    - web/web_guide.md
