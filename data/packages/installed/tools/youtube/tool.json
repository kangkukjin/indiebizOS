[
  {
    "name": "download_youtube_music",
    "description": "YouTube 영상에서 음악을 MP3로 다운로드합니다",
    "input_schema": {
      "type": "object",
      "properties": {
        "url": {
          "type": "string",
          "description": "YouTube URL"
        },
        "output_path": {
          "type": "string",
          "description": "저장 경로 (선택). 기본값은 output.mp3이며, 바탕화면에 저장됩니다."
        }
      },
      "required": ["url"]
    }
  },
  {
    "name": "get_youtube_info",
    "description": "YouTube 영상의 제목, 채널, 길이, 조회수, 설명 등 메타데이터를 조회합니다.",
    "input_schema": {
      "type": "object",
      "properties": {
        "url": {
          "type": "string",
          "description": "YouTube URL"
        }
      },
      "required": ["url"]
    }
  },
  {
    "name": "get_youtube_transcript",
    "description": "YouTube 영상의 자막 텍스트를 추출합니다. 언어 지정 가능.",
    "input_schema": {
      "type": "object",
      "properties": {
        "url": {
          "type": "string",
          "description": "YouTube URL"
        },
        "languages": {
          "type": "array",
          "items": {"type": "string"},
          "description": "선호 언어 코드 목록 (예: ['ko', 'en']). 생략시 자동 선택"
        },
        "include_timestamps": {
          "type": "boolean",
          "description": "True면 [MM:SS] 형식의 타임스탬프 포함. 기본값: False"
        },
        "merge_segments": {
          "type": "boolean",
          "description": "True면 짧은 자막을 60초 단위로 병합. 기본값: False"
        },
        "max_length": {
          "type": "integer",
          "description": "반환할 자막의 최대 문자 수. 생략시 제한 없음"
        }
      },
      "required": ["url"]
    }
  },
  {
    "name": "list_available_transcripts",
    "description": "YouTube 영상에서 사용 가능한 자막 언어 목록을 가져옵니다.",
    "input_schema": {
      "type": "object",
      "properties": {
        "url": {
          "type": "string",
          "description": "YouTube URL"
        }
      },
      "required": ["url"]
    }
  },
  {
    "name": "summarize_youtube",
    "description": "YouTube 영상의 자막을 가져와서 AI로 요약합니다. HTML 파일로 저장됩니다.",
    "input_schema": {
      "type": "object",
      "properties": {
        "url": {
          "type": "string",
          "description": "YouTube URL"
        },
        "summary_length": {
          "type": "integer",
          "description": "요약 길이 (기본 3000자). 예: 1000, 2000, 3000, 5000"
        },
        "languages": {
          "type": "array",
          "items": {"type": "string"},
          "description": "선호 언어 코드 목록 (예: ['ko', 'en']). 생략시 자동 선택"
        }
      },
      "required": ["url"]
    }
  }
]
