[
  {
    "name": "download_youtube_music",
    "description": "YouTube → MP3 다운로드. 한 번 호출로 음악 파일 저장.",
    "input_schema": {
      "type": "object",
      "properties": {
        "url": { "type": "string", "description": "YouTube URL" },
        "output_path": { "type": "string", "description": "저장 경로 (기본: 바탕화면/output.mp3)" }
      },
      "required": ["url"]
    }
  },
  {
    "name": "get_youtube_info",
    "description": "YouTube 영상 메타데이터 조회 (제목, 채널, 조회수 등). 한 번 호출로 모든 정보 반환.",
    "input_schema": {
      "type": "object",
      "properties": {
        "url": { "type": "string", "description": "YouTube URL" }
      },
      "required": ["url"]
    }
  },
  {
    "name": "get_youtube_transcript",
    "description": "YouTube 자막 텍스트 추출. 한 번 호출로 전체 자막 반환. 언어/타임스탬프 옵션.",
    "input_schema": {
      "type": "object",
      "properties": {
        "url": { "type": "string", "description": "YouTube URL" },
        "languages": { "type": "array", "items": {"type": "string"}, "description": "선호 언어 (예: ['ko', 'en'])" },
        "include_timestamps": { "type": "boolean", "description": "[MM:SS] 타임스탬프 포함 (기본 false)" },
        "merge_segments": { "type": "boolean", "description": "60초 단위 병합 (기본 false)" },
        "max_length": { "type": "integer", "description": "최대 문자 수 (생략시 제한 없음)" }
      },
      "required": ["url"]
    }
  },
  {
    "name": "list_available_transcripts",
    "description": "YouTube 자막 언어 목록 조회. 한 번 호출로 사용 가능한 언어 반환.",
    "input_schema": {
      "type": "object",
      "properties": {
        "url": { "type": "string", "description": "YouTube URL" }
      },
      "required": ["url"]
    }
  },
  {
    "name": "summarize_youtube",
    "description": "YouTube 영상 AI 요약. 한 번 호출로 자막 추출→요약→HTML 저장.",
    "input_schema": {
      "type": "object",
      "properties": {
        "url": { "type": "string", "description": "YouTube URL" },
        "summary_length": { "type": "integer", "description": "요약 길이 (기본 3000자)" },
        "languages": { "type": "array", "items": {"type": "string"}, "description": "선호 언어" }
      },
      "required": ["url"]
    }
  }
]
