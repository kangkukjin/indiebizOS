# 언어 보호 선언. 배포 출처(core_manifest) 및 사용자 활성 원장과 별개다.
version: 1
standard_nodes: [self, others, table]
required_packages:
  ibl-core: IBL 실행 기반
  system_essentials: 파일·스크립트·자기관리와 기본 표 출력
  data-ops: 파이프 문법이 사용하는 통화 변환
  visualization: 표준 차트 출력
