# 내부 관용구: AI팁보고서쓰기

## 범위

유튜브 자료 검색부터 원문 보존, 두 차례 추출, 이번 후보의 선발, 내용 검토,
편집, 최종 검수, 로컬 보고서·원장 저장까지 연결한다.
노출 관용구로 승격하지 않는다. 스케줄·공개 업로드·메시지 발송은 포함하지 않는다.

정본은 `data/idioms/ai_tips_report.ibl`, 결정론 부품은
`data/scripts/ai_tips_report.py`이다. 새로운 IBL 액션은 만들지 않는다.
기존 판본 1 관용구의 보수 개정이다. 호출 서명·판본을 바꾸지 않으며 실행 상태 v3는 IBL 판본 2와 다르다.
모델 호출은 IBL의 self:struct/table:ai가 실행 축 모델을 사용한다.
검토를 한 번의 요약 호출로 대체하거나 모델 등급을 낮추지 않는다.

## 호출 계약

호출 이름은 `AI팁보고서쓰기`, 입력 슬롯은 `설정` 객체 하나다.

| 키 | 의미 |
| --- | --- |
| topic | 이번 주제. 검색·추출·선정·편집의 입력이며 고정된 AI 분야 이름이 아님 |
| date | 보고서 기준일 YYYY-MM-DD. 영상 업로드 날짜를 직접 확인해 이전 180일로 제한 |
| root | 절대경로. 기존 _covered_videos.json과 db/tips.json을 보존·갱신하는 위치 |
| mode | draft(기본): 실행별 검수 초안만 저장. commit: 검수 통과 후 지정 root 원장·보고서에 반영 |
| run_id | 같은 회차의 재진입 식별자. 생략 시 날짜·주제·상태 판본 v3에서 결정 |
| reader_context | 독자 수준과 환경의 확인된 사실. 없는 구현 상태를 추측해 채우지 않음 |

결과는 items 1행: status(reviewed_draft 또는 committed), report(절대경로),
evidence(실행 상태·검토 근거), report_hash, new_tips, tips, videos, candidates,
checked. published는 false다. 전체 통과 전에는 성공 영수증을 내지 않는다.

draft를 시작한 run의 mode를 commit으로 바꿔 부르지 않는다. 실행 설정은 불변이다.
같은 root/run_id/설정으로 다시 부르면 완료 단계의 입력을 비워 검색·추출을 반복하지
않고 최초 미완료 단계부터 이어 간다. 유효한 최종 내용 검수가 탈락하면 관용구가 회차당
한 번 자동 보완한다. 기존 판정·본문·실패 입력을 revision-N 파일에 보존하고, 탈락 이유를
전달해 영상별 내용 검토·편집·최종 검수를 다시 수행한다. 수집·추출 결과는 재사용한다.
다시 탈락하면 중단하며 재진입으로 자동 보완 횟수를 초기화하지 않는다. 원인을 별도로
보완한 뒤에는 부품의 op=revise, from=reviewed와 reason으로 명시적 재검토를 시작할 수 있다.
완료된 보고서나 조사 입력 변경은 새 run에서 검토한다. 저장 도중 중단은 같은 run으로 복구한다.

## 품질 경계

- 한국어·영어·구체 도구·회의적 관점·실행 동사 다섯 층으로 검색한다.
- 선정 2~4편, 60분 초과 최대 1편. 검색 결과의 상대 날짜로 업로드 날짜를 추측하지 않는다.
- 후보 영상의 메타데이터로 먼저 출처·인기를 판단하고 선정된 영상에만 자막 추출을 요청한다.
  발행 채널/ID/URL·업로더·설명 발췌·조회수·좋아요·댓글·구독자·인증·조회 시점을 보존한다.
  미제공 수치는 null로 두며 실제 0과 구별한다. 설명은 자기주장, 인증은 신원 신호로 취급한다.
  선정 AI는 출처의 전문성·직접 시연·근거와 업로드 경과일·주제·언어·채널 규모를 함께 판단한다.
  고정 인기 점수로 순위를 정하지 않는다. 인기만으로 정확성·효용을 확정하지 않는다.
- 팁 비교·선발에도 video_id로 연결한 출처·인기 정보를 전달한다. 채택 팁에는 출처 판단,
  인기 판단, 실행 가능성·유용성의 가치 판단을 요구한다. 독립 검토와 최종 검수에도 이를
  전달하며, 보고서에는 영상별 지표·판단 및 팁별 가치 판단을 남긴다.
- 자막 전문과 원본 봉투를 실행 폴더에 남긴다. 스필 참조를 회수하며 미리보기로 대체하지 않는다.
- 1차는 제목·시점, 2차는 선정 팁의 방법·도구·조건을 추출한다. 두 번 모두 grounded=true.
- 각 인용문을 원문과 대조하고 시점도 확인한다. grounded 탈락·인용 누락·시점 누락·부분 실패를 숨기지 않는다.
  시간 대조는 연속 자막에서 인용문 전체의 시작 위치를 찾는다. 두 자막 줄의 중간을 잇는
  인용도 허용하지만, 근처의 짧은 공통 단어만으로 다른 위치의 인용을 승인하지 않는다.
- 영상별 할당량은 없다. 8~15개를 목표로 하지만 근거 없는 팁으로 수를 채우지 않는다.
- 독립 내용 검토는 전달 범위가 표시된 자막 원문과 추출 행을 다시 비교한다. 외부 공식 근거가 필요하면 needs_evidence로 멈춘다.
- 최종 본문·요약·출처·건수·원장 갱신은 같은 최종 팁에서 파생한다.
- 검수는 두 층이다. 독립 검토가 자막 원문 대조를 담당하고, 최종 검수는 그 판정·근거 행과
  편집된 본문을 비교한다. 검색·실측 메타데이터·누적 원장 건수도 전달해 인계 누락을 막는다.
  최종 항목은 근거·실행 가능성·이번 보고서 내부 중복·조건·출처·편집 해석 분리·건수다.
- 한계 문구를 붙이는 것으로 실행 가능성 부족을 대신하지 않는다. 구체 행동이나 선택 조건이
  없는 추상적 원칙은 팁에서 제외한다. 누적 유무와 무관하게 이번 팁끼리의 중복만 검토한다.
- 검수 지문, 저장 본문, 보존 자막을 다시 대조한다. 원장이 조사 중 달라지면 반영을 거부한다.
- 반영은 공유 파일 잠금과 전후 내용 저널을 사용한다. 원장들을 먼저 쓰고 보고서를 마지막에 쓴다.
  여러 파일의 물리적 단일 트랜잭션은 아니며 중단된 쓰기는 다음 동일 run이 복구한다.

주제만 바뀌고 위 조사 방식·결과 계약이 같으면 같은 관용구를 재사용한다.
주식 전망·법률 판단처럼 근거 소스와 검증 방식이 달라지면 topic만 바꾸어 적용하지 않는다.

## 현재 한계

출처·인기 판단은 추가 AI 단계 없이 기존 선정·검토에 합쳐진다. 영상 정보 조회도 기존
yt-dlp 호출 결과를 재사용한다. 설명문은 후보 수에 맞춘 길이 제한과 발췌 표시를 가지며,
후보 자체나 신원·수치는 입력 절약을 위해 누락하지 않는다. 채널 설명만으로 외부 경력과
평판을 검증한 것은 아니다. 새 실행은 selection_policy=2를 기록한다. 이미 선정이 끝난
옛 실행의 영수증은 보존하며 새 판단을 소급했다고 주장하지 않는다.
설계·검증: [출처와 인기의 인계](AI_TIPS_SOURCE_SELECTION_2026_09_24.md).

긴 자막은 영상 전체를 단일 AI 요청에 넣지 않는다. 시간 표기를 포함한 원문이 36,000자를
넘으면 `ai_tips_sources.py`가 전 구간을 나눠 추출하고, 후보별 관련 위치를 전체 구간에서 색인한다.
경계 양옆 문맥을 겹쳐 읽으며 핵심 구간의 누락·중복은 인덱스로 검사한다. 큰 단일 세그먼트도
원래 시간·문자 오프셋을 유지해 나눈다. 구간별 추출·색인 영수증을 즉시 저장하여 재개 시 성공한
형제 호출을 반복하지 않는다. 전 구간 처리가 끝나야 후보 비교로 진행한다.
색인은 방법·조건·예외·반례·철회·참조 위치를 고르며 모델이 쓴 요약을 원문 대신 사용하지 않는다.
선택 위치의 실제 자막을 모아 후보별 상세 추출·불확실한 신규성 보완·독립 내용 검토에 전달한다.
짧은 원문의 전문 전달 계약은 유지한다. 긴 원문의 후속 검토는 전체 구간 색인으로 찾은 관련
원문을 읽는 방식이며, 전달 범위와 모델의 위치 선택을 보고서·검수 입력에 명시한다.
위치 선택은 의미 판단이므로 완전한 재현율의 증명은 아니다. 불확실한 색인은 승인할 수 없다.
개별 AI 요청의 57,000자 상한은 유지한다. 한 후보의 관련 원문 자체가 36,000자를 넘거나
후보·원장 한 행만으로 요청 상한을 넘는 경우에는 저장 결과를 보존하고 범위 보완을 요구한다.
이 경우에도 근거를 조용히 잘라 성공으로 처리하지 않는다.
전문 검토 입력은 모든 구간의 시작 시점·원문을 순서대로 문자열에 보존한다. 반복 JSON 키만
없애며 요약·절단하지 않는다. 원본 구간 객체·길이·지문은 실행 폴더에 별도로 유지한다.
## 실행 경계 v3 (2026-09-24)

중복 방지 단위는 영상 ID다. 검색 단계에서 `_covered_videos.json`의 ID를 제외한다.
이미 다룬 영상의 메타데이터·자막을 다시 요청하지 않는다. 과거 보고서와 같은 팁은 허용한다.
누적 팁 제목의 추출 주입, 전건 의미 비교, 비교 실패의 전문 재검토, 비교 근거의 후속 전달을
제거했다. DB는 누적 보존·건수 계산·저장 충돌 검증에 사용하며 AI 입력으로 보내지 않는다.
최종 검수의 `within_report_uniqueness`는 이번 보고서 안의 중복만 확인한다.

단계: start → queries → search → metadata → videos → transcripts → candidates →
chosen → details → reviewed → draft → finish. candidates 다음의 source_requests →
source_scanned(긴 원문 구간별) → sources_indexed는 원문 근거를 보존하는 경계이며,
짧은 원문은 추가 AI 호출 없이 통과한다. sources_indexed가 이번 후보 선발 요청을 반환한다.

영수증에 버전·설정/원장 범위·입력·출력 지문을 붙인다. 검증된 입력은 다음 요청 준비보다
먼저 확정한다. 준비 실패는 blocked·accepted·evidence·resume를 반환하고 상태에도 남긴다.
재개 시 완료된 외부 호출·추출·원문 색인을 반복하지 않는다. 유효한 내용 검수 탈락만 1회
자동 보완하며 원장 충돌·원문 변경·잘린 입력은 자동 통과시키지 않는다.
추가 공식 근거가 필요하면 op=evidence(run,candidate_ids,data)의 실제 출처 URL·text를
연결하고 다시 검토한다. 자동 검색·HTML 공개·알림은 관용구 밖이다.

v1/v2의 상태·검수 영수증은 자동 변환하지 않는다. 명시적으로 옛 run을 재개하려 하면
새 run_id를 요구한다. 기본 run_id에 -v3를 붙여 새 이름 호출이 옛 비교 경로에 닿지 않게 한다.
새 실행은 이전 실행의 미반영 조사 자료를 자동 가져오지 않는다. 운영 원장에 반영된 영상은
계속 제외한다. 중단된 운영 보고서는 수리 과정에서 재시작하지 않는다.

## 검증과 변경 이력

`.venv/bin/python -m pytest data/scripts/test_ai_tips_report.py -q`

실제 IBL 파서·이름 호출·each·중첩 제한·결정론 부품을 사용하고 AI/YouTube 응답은 고정한다.
출처/인기 인계·근거/시점·부분 실패·저장 충돌·자동 내용 보완·완료 재진입을 검증한다.
누적 0/655/2,000팁에서 모델 호출 수가 같고 과거 본문이 AI에 전달되지 않으며, 같은 내용의
새 영상 팁도 저장되고 과거 영상 ID는 조회 전 제외되는지를 검사한다.
실제 모델의 총 토큰·완료 시간·선발 품질은 이 고정 응답 검증만으로 입증하지 않는다.

과거 전건 비교와 복구의 구현 기록은 현재 계약이 아니다:
[4010 당시 수리](EPISODE_4010_REPAIRS_2026_09_24.md),
[전건 비교 제거의 원인과 검증](AI_TIPS_VIDEO_ONLY_DEDUP_2026_09_24.md).

## 재등록

에피소드 4011 이후 검색은 각 질의의 봉투를 보존한다. 정상 0건은 수용하고 실제 오류만
`search_retry`(인자 run)로 한 번 자동 재시도한다. 성공한 질의는 단계 중 일부가 실패해도
다시 부르지 않는다. `search_outcomes`에는 원 응답·시도별 지문·결과 지문이 남으며,
옛 평탄화 실패 입력도 지문을 확인해 성공한 질의부터 회수한다. 옛 오류 문구를 분석해서
0건으로 간주하지 않고 해당 질의를 새 계약으로 다시 실행한다. 상세: [4011 수리](EPISODE_4011_SEARCH_REPAIR_2026_09_24.md).

스크립트는 self:script register의 ID `AI팁보고서`로 등록한다.
관용구는 `scripts/register_idiom.py`로 위 IBL 파일을 add/update한다.
topic은 `보고서/유튜브 AI 팁`. always_on 승격 옵션을 주지 않는다.
등록 DB는 로컬 실행 저장소이며 재현 가능한 본문은 Git에 보관한다.

긴 자막 수리의 구조·재현·검증: [4011 후속 수리](EPISODE_4011_LONG_SOURCES_2026_09_24.md).

연속 자막 인용의 시간 대조와 추출 실패 후 후속 호출 차단:
[4016 수리](EPISODE_4016_GROUNDING_REPAIR_2026_09_24.md).
