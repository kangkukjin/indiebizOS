# 에피소드 3949~3951 수리 — 2026-09-21

## 관측과 변경

- **3949 입력 선별**: 선행 마침표 `.`가 사용자 사실 후보가 되어 무관한 기억을 검색했고,
  작은 구획 우선 배치로 실행·공간 자료는 예산 초과로 제외됐다. 문장부호 단위의 원문·ID는
  유지하되 저장 후보에서 제외한다. 통합 입력은 실행 → 심층 → 공간 순으로 배치하고,
  넘치면 분류 목차를 먼저 덜어낸다. 원문·비교 본문·검수 근거는 자르지 않는다.
  종류별 제외 사유 자체의 크기도 예산에 포함한다.
- **3950 회상 사용**: 블로그 갱신·최신 글 읽기에 목록 조회·HTML 생성까지 5/5 사용으로
  표시됐다. 공통 파서의 호출 순서와 `op/mode/source/store/format/do`를 대조하며,
  복합 용례의 일부 액션만 등장해도 전체 사용으로 세던 판정을 제거했다. 실제 사례는
  갱신과 최신 글 선택 **2/5**다. `record_recall_outcome`의 top-1 귀속도 같은 함수를 쓴다.
  문자열·주석·지연 본문을 직접 호출로 세지 않고, 해석하지 못한 제어 블록의 완주를 추정하지 않는다.
- **세 턴의 로그**: 통합 선별 후 피드백만 처리하려고 `write_deep=False`로 재호출했는데
  이를 발화자 부적격으로 설명했다. 피드백 재호출은 그 로그를 생략하고 실제 통합 증류 상태를
  출력한다. 세 턴의 과거 저장 0건은 그대로이며 특히 3951은 사용자 원문을 검토한 뒤
  지속 가치가 불확실하다는 모델 판단이었다.
- **능력 검사 후보**: `수업` 같은 단어의 첫 글자를 `수 없다` 구문으로 잡지 않게 했고,
  “문맥을 충분히 반영하지 못했습니다” 같은 과거 해석 정정 구문은 후보에서 제외한다.
  같은 답변의 별도 능력 부정은 계속 검사한다. 모든 메타 대화를 규칙으로 판별하지 않는다.

## 검증 범위

`test_episode3949_3951_repairs.py`는 문장부호 후보·예산 우선순위·목차 생략·원문 보존,
블로그 2/5 집계·역순/부분/인용/지연 호출·피드백 로그·능력 부정 보존을 고정한다.
기존 통합 증류·회상 사용·능력 검사·기억 위생 회귀도 함께 실행한다.

전체 백엔드 5,320개를 실행해 새 시험 파일의 직접 실행 진입점 누락 한 건을 발견했다.
`__main__`에서 pytest로 위임하도록 수정한 뒤 해당 저장소 관문을 포함한 관련 회귀
112개가 통과했다. 새 파일의 직접 실행도 12개 시험을 실제 수행해 통과했다.
층 구조 검사·Android 몸 번들 최신성 검사·diff 공백 검사를 통과했다.

저장된 세 턴의 입력·도구 기록을 읽기 전용으로 재현했다. 실행 비교 용례는 재현 시점의
현재 코퍼스, 분류 목차는 3950에 보존된 자료를 썼다. 실행 구획은 기존 관문으로 다시
준비하고 심층 자료는 당시 준비본을 사용했다. 공간 구획 재구성과 모델 재선별·저장은
이 확인에 포함하지 않았다.

| 에피소드 | 포함 구획 | 입력 UTF-8 바이트 | 실행 원문 수 |
|---|---|---:|---:|
| 3949 | 실행 | 20,245 | 14 |
| 3950 | 실행 | 15,019 | 3 |
| 3951 | 심층 | 22,625 | 0 |

이는 자료가 판단 모델에 도달할 수 있다는 검증이다. 실제 저장 가치나 저장 건수가
증가한다는 주장은 아니다. 긴 원문 구획은 여전히 예산 때문에 제외될 수 있으며 원장은
보존한다. 과거 에피소드·증류 결정·기억을 자동으로 다시 쓰지 않는다.
