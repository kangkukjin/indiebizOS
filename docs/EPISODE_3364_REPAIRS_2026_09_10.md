# 에피소드 3364 수리 — 2026-09-10

후속: 9월 11일 통합 수리에서 아래의 초기 4,000자 고정 페이지는 12,000자로 조정했고, 감독 예산은 기본 소프트 단계 배분으로 바뀌었다. 현재 계약은 [통합 수리 기록](AGENT_EFFICIENCY_REPAIRS_2026_09_11.md)을 따른다. 아래 수치·산출물 검증은 3364 후속 수리 당시 기록이다.

대상은 스토리텔러의 글 7891410 영상 제작(`task_7faf09ca`,
`run_e43efe0fcf787d84b24c`)이다. 사용자 요청에 따라 감독·기억·비용 기록과 실제 영상을 고쳤다.
당시 사건과 UNKNOWN 판정은 보존한다. 후속 수리를 당시 감독 승인으로 소급하지 않는다.

## 확인한 문제와 수리

| 문제 | 변경 |
|---|---|
| 감독의 메모리 조회가 자원 소유자 대신 임시 의식 ID를 사용해 실패 | 도구 실행 동안 소유자 actor context를 적용하고 호출 후 의식 ID 복원 |
| 계획/검수의 예산 중단이 CLI의 ‘사용자 취소’로 표시 | 취소·시간·토큰 예산 원인을 따로 기록하고 최종 UNKNOWN에도 원인 유지 |
| 계획 입력 253,964토큰이 일반 몫 220,000을 넘어 최종 검수 몫까지 잠식 | 최종 예약 몫 보호, 미사용 일반 몫도 활용. 라운드 단위 초과 청구는 실제 비용에 포함 |
| 큰 도구 결과와 전체 도구 스키마 반복 주입 | 4,000자 페이지와 증거 참조로 반환. 스키마는 필요한 도구만 조회. 허용하지 않는 제어 도구는 목록에서 제외 |
| IBL `&` 자식 이미지 호출의 토큰이 턴 원장/요약에서 누락 | 각 병렬 작업에 독립 `copy_context()`를 주어 부모 에피소드와 토큰 원장 공유 |
| UNKNOWN이 목표 실패로 취급되고 큐에서 상세 상태 소실 | 상태·중단 이유·task를 영속 큐에 보존. UNKNOWN은 학습 보류이며 심층 기억에도 미검수 상태를 전달 |
| 기억의 원 발화 절단, task 공란, UPDATE 보충 출처 누락 | 전체 발화·task/episode 출처와 보충 출처 보존. 단일 영상 조건을 지속 선호로 일반화하지 않도록 명시 |
| 원고·노트·materials 사본 불일치, 반복 Python 길이 계산/음량 조정 | `나레이션원고추출`의 source/apply_notes/inspect·길이 추정, `영상음량정렬` 등록 스크립트와 IBL 절차 제공 |
| 독립 영상 렌더 자식에서 층 경로를 찾지 못함 | `deck_video.py`의 독립 진입에 `boot_paths` 적용 |

검수 미완료 자료는 `data/spill/supervision/<turn-id>/review_status.json`에 원래 목표·응답
버전/지문·이유·학습 상태로 남는다. 다음 요청에서 이 파일과 같은 작업대의 증거·현재 산출물을
읽어 다시 검수한다. 과거 판정을 덮거나 파일 존재만으로 자동 승인하지 않는다.

## 과거 기록 정정

원본 백업: `data/_backups/2026-09-10_episode3364_repairs/`.
라이브 SQLite는 SQLite backup으로, 강의 폴더·원고·기억 트리도 수정 전에 보존했다.

- `trajectory_event`의 첫 `run.ended`(seq 460) 이전 모델 사건으로 요약의 steps를 재구성한다.
  토큰은 **6,967,136 → 7,053,764**. 누락된 이미지 원샷 6건 **86,628토큰**을 복원한다.
  입력에는 캐시 읽기가 포함되므로 이 합계를 실제 과금액과 혼동하지 않는다.
  종료 뒤 후처리 14,934토큰은 이번 턴 합계에서 분리한다.
- 원 사건·원문 IBL·22 실행 라운드·시간·UNKNOWN 판정은 유지한다.
  당시 plan/final 중단 원인은 토큰 스냅샷에 근거한 **예산 중단 추정**으로 표시한다.
- 심층 기억 123은 처음 결과와 후속 수정본을 구분한다. 118의 이번 작업 선호 보충을 제거하고,
  124는 이번 영상의 작업 조건으로 범위를 한정한다. 세 기억은 정정 출처를 보존하고 트리/검색 색인을 갱신한다.
- 현재 증거와 파일 SHA-256은 `outputs/episode3364_repair_evidence.json`에 저장한다.

## 영상 수정본

- s007에 ‘모두 혼자 독립할 필요 없음’, ‘작고 서로 연결된 공동체와 플랫폼’, ‘다원주의’를 복원했다.
  수정 장만 kkj3로 다시 생성(4조각)했으며 원격 Colab 자원 반납을 확인했다.
- 11장·저자 1인칭·실음성 인사말을 유지한다. 굽는 본문은 2,056자이며 원고·노트·자료 사본을 동기화했다.
- s005의 문장 끝 ‘다.’만 떨어진 조판을 두 줄로 고쳐 PNG를 직접 확인했다.
- 수정본: `outputs/lectures/jigjanggwa-jugeobi-geurigo-yugarobuteoyi-haebang/video/episode3364_repaired_final.mp4`.
  321.572993초(5분 22초), 1280×720 H.264/AAC, 18,646,375바이트.
  정적 게인 +5.6dB, −22.3 LUFS / −1.0 dBTP. 피크 상한 때문에 −21 LUFS 목표보다 1.3dB 낮다.
  음량 압축·리미터·재생속도 변경은 적용하지 않았다.
- 신호 검사는 전체 0–321.573초, 모델 청취는 전체 3구간과 분석 경계 앞뒤를 다룬다.
  전사·신호 측정·모델의 음질 판단은 각각 별도 증거다. 상세는 `outputs/episode3364_audio_qa.json`.
  모델 청취는 사람의 최종 취향 평가나 목소리 동일성 보증을 대신하지 않는다.

## 검증

신규 회귀는 자원 소유자/복원, 병렬 비용/에피소드/턴 격리, 예산 예약,
UNKNOWN/큐/심층기억 전파, 큰 오류 결과 페이지, 제어 도구 노출 제외,
출처 보존, 원고-노트-사본 일치, 인코딩 후 피크 재측정을 검사한다.
전체 backend 회귀와 어휘/층/Android 번들 관문을 실행한다.
로컬 서버·문서 렌더러 검사는 샌드박스에서 차단되어 허용된 환경에서 다시 실행했다.

감독 토큰 절감과 IBL 재사용률의 향상은 다음 실제 주행에서 측정해야 한다.
이번 작업은 결함 재현·회귀·기존 산출물 수리까지 확인하며, 과거 주행의 개선된 비용을 가정하지 않는다.
