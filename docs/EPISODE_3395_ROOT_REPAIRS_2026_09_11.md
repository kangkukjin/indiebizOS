# 에피소드 3395 이후 공통 실행 경계 수리

정본 `/Users/kangkukjin/Desktop/AI/indiebizOS`에서 작업했다. 기존 보고서는 수정하지 않았다.
감사에서 확인한 반복 오류를 원문 → 변환 → 검수 → 기억 → 재사용의 경계에서 수정했다.

## 원인과 변경

| 반복 현상 | 끊어진 연결 | 이번 변경 |
|---|---|---|
| 요약에서 증가율이 규모로 바뀌고 매체 귀속이 틀림 | 검수가 본문 읽기·형식 범위만으로 승인 가능 | 텍스트 산출물의 meaning/sources/counts 검수를 명시적으로 요구. 본문 지문·전체 읽기·실제로 읽은 원문 발췌를 대조하고 부족하면 보완 |
| 검증 후 강등·중복이 NEW 합계에 미반영 | 후보 표와 최종 편집 결과가 서로 다른 정본 | 최종 행 JSON을 보관하고 조건·식별자·계수를 하네스가 재계산. 가이드의 발행 게이트를 원문 검증·사건 중복 제거 뒤로 이동 |
| 변환 결과가 옛 통과 판정·행수를 승계 | 검사 대상 행의 판본이 사라짐 | 행 내용이 바뀌면 criteria와 AI 행수 메타를 현재 결과에서 떼어 상류 입력 지문과 함께 보존 |
| 자동 기억이 주체·행동을 재작성하고 정책을 사용자 결정으로 저장 | 앞 500자만 읽고 추출·중복 판정에서 각각 자유 작문 | 전체 문단에 번호를 부여하고 모델은 번호만 선택. 저장 문장은 원문에서 구성. AI 응답은 작업기록으로 제한하고 미승인 응답은 자동 사실 기억에서 제외 |
| 기억 정리에서 다시 사실 변형 | 병합·압축도 새 문장을 작성 | 정리도 원문 선택으로 통일. 상대 날짜를 현재 날짜로 재해석하지 않고 기록 시점·출처 보존. 수정·병합에 원문 비교 조건을 걸어 동시 변경 덮어쓰기 차단 |
| 실행은 성공했지만 증류 구문 복구 뒤에도 학습 0 | 실행 코드 재작성, 전체 작업과 부분 절차의 구분 부재 | 첫 요청부터 성공 문장 번호 선택. 변수 생산자 자동 포함. 첨부 본문에 의존하는 꼬리를 제외한 독립 부분 절차도 저장 |
| 부분 절차 학습이 전체 작업을 대체할 위험 | 유사도만으로 자동 반사 | 부분 용례는 `distilled_component`, 반사 임계 미만으로 제한. 전체 턴 비용을 부분 용례의 실측 비용으로 귀속하지 않음. 자동 함수 작명은 계속 중단 |
| 독립 읽기·AI 원샷이 직렬 실행 | 병렬 가능 여부를 매번 모델이 결정 | each 기본 auto: 사전에 읽기 전용으로 선언된 몸만 최대 4개 동시 실행. 쓰기·미분류·stop은 순차, 명시 parallel:1 존중. 입력 순서·실패·예산 계약 유지 |
| 정상 진행 중 비용 점검이 작동하지 않음 | 장시간 점검에도 정체 조건 필요 | 비용 점검을 정체와 분리. 실제 진행이 있어도 주기·최대 횟수 안에서 점검. 기다리기만 하는 정상 장기 작업은 제외. 전체 토큰·캐시·실행 비용을 상태에 제공 |
| 한글 파일 크기를 바이트로 잘못 보고 | 문자열 길이를 파일 크기로 반환 | write의 size·원장·spill bytes를 UTF-8 바이트로 계산, chars는 별도 반환 |

구문·union·태그 길이 거절 자체는 유효한 관문이었다. 이를 느슨하게 하거나 잘라서
성공으로 위장하지 않았다. 원문 선택 학습과 기존 check:true 정적 검사를 사용하며,
태그는 길이 제한 안의 명사구로 작성하고 반복 실패를 새 방법론 규칙으로 누적하지 않는다.
가이드·규칙 원장에 실행 이력을 쌓지 않는 규약은 유지한다.

## 기존 기억의 오염 제거

라이브 메모리 DB는 SQLite backup으로 `data/_backups/2026-09-11_episode3395_memory/`에
보관한 뒤 처리했다. #528의 잘못된 기관 행동·바이트 수·NEW 계수 의미를 감사 근거로
정정하고 재인덱싱했다. #527의 중복 정책 기억은 사용자가 내린 결정으로 잘못 분류된
자동 추출이므로 삭제했다. 규칙의 정본인 가이드는 보존하고 개정했다.

기존 보고서의 SHA-256은 전후 동일하다:
`456f973a34585e046c402aad7b23849cf28ea85476b44d7397de460a93f275dc`.

## 검증 범위

재현 시험은 원문 뒤쪽 보존, 사실 재작성 무시, 기억 분류·정리, 원문 비교 갱신,
출처 보존, 독립 부분 학습, 미확인·중복 사건 계수, 읽지 않은/변경된/발췌 불일치
산출물 승인 거절, 자동 병렬의 쓰기 제외, 상류 검사 분리, UTF-8 크기를 다룬다.
검증 결과: 전체 backend 회귀 **3,576개 통과**(종료 코드 0). 최종 기억 갱신 보완 뒤 관련 회귀 75개도 통과했다. IBL 파생물 `--check`, 층 검사, Android 번들 생성, `git diff --check` 통과. 샌드박스에 막힌 로컬 서버·렌더 검사는 승인된 정상 권한으로 재실행했다.

파일 지문·발췌 존재·계수는 결정론적으로 확인한다. 의미 일치·사건 식별·검수 범위의
해당 여부는 여전히 모델 판단이며 모든 사실 오류가 불가능해졌다는 보장은 아니다.
새 보고서를 생성하는 실서비스 실행이나 전후 시간·요금 비교는 수행하지 않았다.
따라서 특정 절감률은 주장하지 않는다. 모델 기어와 산출물 품질 기준은 낮추지 않았다.
