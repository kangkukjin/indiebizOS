# 에피소드 3507–3510 수리 — 2026-09-11

정본 `/Users/kangkukjin/Desktop/AI/indiebizOS` main에 반영하는 수리다. 감사 대상과 비용 기준은 [원본 감사](EPISODE_3507_3510_AUDIT_2026_09_11.md). 모델 기어나 설정 가격을 낮추지 않았고, 수리 검증을 위해 외부 서비스나 AI를 다시 호출하지 않았다.

## 적용한 계약

| 감사 문제 | 구현·복구 |
|---|---|
| 3510: 식사 장소 질문 전에 외장 볼륨 탐색에서 정지 | 선택적인 전경 `disk_skeleton` 호출을 제거했다. 파일 도구는 명시적으로 사용할 수 있다. 회상 단계별 시작·끝·장기 대기를 기록하고 대기 감시가 새 모델을 부르지 않는다. |
| 3507: 과거 여행과 AI의 틀린 동행자 표를 현재 사실로 사용 | 검색·회상·원문 읽기에 발화자와 사건 시점을 붙인다. 관련성은 현재 사실의 증명이 아니다. 자동 증류는 사용자 사실·선호·결정의 원문만 선택한다. |
| 3508: 100자 미리보기 뒤 예약 사실 누락, 중복 본문 | 질의어 주변의 발췌, 잘림 여부, 원문 길이·위치를 제공한다. 회상은 문서 소개와 기억 본문을 각각 한 번만 반환한다. 빈 가지를 전체 기억 부재로 해석하지 않도록 범위를 표시한다. |
| 3508: 사실 통보가 경로·날씨 조사로 확대 | 기존 분류 호출에 `CONTEXT_UPDATE`를 추가했다. 사전의 `context_update_ops`로 관련 기억 확인·갱신만 허용하며 실행자와 감독의 직접 도구 양쪽에서 집행한다. 추가 분류 AI 호출은 없다. |
| 3508: 독립 조회 결과 소실·여행일 밖 예보·등록 없는 재확인 약속 | 실행 규약에 독립 결과 보존, 요청 기간 대조, 실제 등록 영수증과 후속 약속 대조를 넣었다. 검수도 같은 계약을 확인한다. 이 의미 판단은 모델 검수의 몫이며 완전한 기계 보장은 아니다. |
| 3509: 산술 오류를 AI가 다시 잘못 수정 | 기존 식 평가기의 제한된 계산 도구를 감독/수정 실행자에 제공한다. 명시적인 두 시간 항의 잘못된 합산 패치는 저장 전에 거절한다. 검수에 시간 단위 표와 오류 근거를 전달한다. 숨은 휴식 가정·근사 일정은 코드가 추측하지 않는다. |
| 3509: local 필드 누락·거대 세션 재개 | REWORK 최상위 `repair_scope`를 검증하고 구 instruction 접두의 local은 호환 해소한다. 응답만 수정할 때는 목표·기준·증거 참조와 작은 시스템 프롬프트, supervision 도구만으로 새 문맥을 만든다. 파일 산출물/실행 중 작업은 기존 안전 경계를 유지한다. |
| 3509: response id 무시·지도와 전체 manifest 반복 | id는 정확한 블록 주소다. 패치 영수증과 후속 검수는 바뀐 블록을 전달한다. 같은 기준·근거에서만 기존 읽기 증거를 재사용하며 의존 주장 검토를 지시한다. 기준이나 근거가 바뀌면 전체 읽기로 되돌린다. |
| 3509: 영업일·방향·휴게소 접근성 과신 | 검색 요약·본문·당일 직접 확인을 구분하고 추정·시각·범위를 주장 옆에 유지하도록 실행/검수 규약을 연결했다. 식당 영업 여부를 새로 확인한 것으로 보고하지 않는다. |
| 후처리: 질문·AI 권고·빈 조회·요청 밖 조사 학습 | 질문뿐인 턴은 기억 추출 AI를 부르지 않는다. 보충 사실은 다른 사건 행으로, 정정은 이전 내용·출처 이력과 함께 저장한다. 사실 통보 경험 증류와 미검수 빈 조회·기억 조회만 있는 경험 증류를 생략한다. 미검수 도구 성공은 전체 목표 성공으로 승격하지 않는다. |

응답 국소 수정의 세션 선택은 가격 추측보다 실제 입력 범위를 먼저 줄인다. 기존 cost 추정은 파일 수정 경로의 휴리스틱으로 남으며 `chars/2`, cache 가중치 .1을 비용·지연 실측으로 주장하지 않는다. Claude CLI 국소 수정은 내장 파일/셸 도구도 비활성화한다.

## 라이브 기억 정정

수정 전 라이브 SQLite 두 개를 `.backup`으로 `data/_backups/2026-09-11_episode3507_3510_repairs/`에 보존했다. 예약 식별자 등 개인정보는 커밋 문서에 복사하지 않았다.

- #55: AI의 세 명 동행자 표를 3510 사용자 발화로 정정했다. 사용자가 말한 본인·두 형님·어머니 네 명이다. 이전 표와 출처는 정정 이력에 남겼다.
- #56: 사용자가 실제로 전달한 숙소 예약 안내 원문과 3509 출처를 연결했다. 당시 경로·좌표 관측은 #59로 분리하고 조회 시각과 확정값이 아님을 명시했다.
- #57·58: 일회성 질문·안내 문구와 미수락 AI 권고는 지속 기억에서 제거했다. 원본 대화·에피소드와 SQLite 백업은 보존했다.
- 경험 #4621·4622: 잘못된 회상/요청 밖 확장을 재사용 절차로 만든 행과 벡터를 제거하고 주제 문서를 갱신했다.
- #14: 출처 미확인인 과거 기록을 임의로 현재 여행 사실로 고치지 않았다. 개선된 조회가 그 상태와 원문을 드러낸다.

변경 영수증은 같은 백업 폴더의 `memory_repair_receipt.json`. 원본을 잃지 않는 1회 정정이며 새 AI 호출은 0이다.

## 재기동 관측과 복구

적용 전 제어기는 FAILED/worker 없음이었지만 실제 구 워커는 살아 있었고 3510만 미완료였다. 구 세대 인증으로 확인한 소유 현황에는 이미 사라진 PID 관측 오류, 시작 요청에 붙어 남은 상주 메시지 루프·진행률 관리 스레드도 있었다.

프로세스 열람 사이 정상 종료는 더 이상 관측 불명 오류가 아니다. 접근 거절은 여전히 관측 불명으로 남긴다. 상주 메시지 루프와 모델의 관리 스레드는 서비스 수명으로 분리하며 실제 메시지·유한 모델 로드의 소유는 유지한다. 새 관리 세대는 이전 legacy 신원 표기를 비운다.

21:42 추가 표본에서는 메인 이벤트 루프도 `os_scandir → open_nocancel`에 멈춰 상태 API가 응답하지 않았다. 디스크 목록을 동기로 읽던 PC 관리·NAS·공유 파일 목록 API를 FastAPI 작업 스레드로 옮겼다. 느린 목록 요청 중에도 health 요청이 끝나는 ASGI 회귀를 통과했다. 외장 볼륨 자체의 OS 지연을 해결했다고 주장하지 않는다.

사용자가 다시 작업 중지를 명시한 뒤 PID+출생 시각, 원장의 3510 단독 미완료와 모델/도구 실행 전 정지를 확인했다. 구 제어기와 워커·터널/자원 관리·재생 보조 프로세스를 종료하고 21:50:27에 3510과 해당 pending task를 중단/실패로 기록했다. 새 제어기 PID 24722, 워커 PID 24723이 ACTIVE로 시작했고 원장 미완료 에피소드는 0건이었다. 질문은 보존했고 자동 재실행하지 않았다. 최종 검증 코드 digest는 `94ad71afeb62d84f505ca4f89646d59ff7ee1ea080ff0757e4646a516aa5ca39`.

이는 사용자 승인에 따른 사건 복구다. 일반 제어기의 UNKNOWN 자동 강제중단 금지는 유지한다. 상태/소유 관측의 실패를 작업 없음으로 간주하지 않았다. 복구 전 원장·대화 DB와 제어 상태를 같은 백업 폴더에 보존했고 결과는 `runtime_repair_receipt.json`에 기록했다.

## 검증과 한계

- 전체 백엔드 회귀: `/tmp/episode3507-full-tests-final.log`, 종료 코드 0. 첫 샌드박스 실행의 로컬 소켓·LibreOffice 제한은 권한 있는 재실행으로 확인했다.
- 이후 추가된 런타임 수명/legacy 표기 수정: 회귀 58개 통과 (`/tmp/episode3507-runtime-tests.log`).
- 어휘 파생물 `build_ibl_nodes.py --check`, backend 층 가드, 은퇴 계약 가드 통과. Android 몸 번들을 재생성했다.
- 호텔 사실이 접두 발췌 밖에 있어도 발견되는지, 원문 중복이 없는지, 발화자 정보가 봉투 압축 뒤에도 남는지 검증했다.
- 큰 기존 프롬프트를 가진 fixture에서도 local 응답 수정이 작은 새 문맥으로 실행되고 원래 실행자의 도구·프롬프트는 보존되는지 검증했다. 특정 블록 읽기·변경 읽기·기준 변경 시 전체 재검토도 확인했다.
- 잘못된 `4시간 45분 + 45분 = 5시간 15분` 패치를 거절하고 330분(5시간 30분)을 허용한다. 경로 151+134=285분, 직행 176분 대비 109분 증가, 휴식·식사·여유를 합친 420분을 코드로 대조한다.

이 검증은 결함 재현과 입력 전송 감소의 증거다. 수정 뒤 실사용 모델의 응답 품질·지연·청구 토큰을 A/B 실측한 절감률은 아니다. 날짜별 영업 확인과 일정 가정처럼 의미 검수가 필요한 영역은 모델 판단이 남는다.
