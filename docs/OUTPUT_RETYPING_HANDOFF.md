# 출력 되받아쓰기 소거 — 모든 턴의 시간 구조에 대한 일반 계획 (핸드오프, 09-06 2판)

> 2026-09-06 작성, 같은 날 2판: 커밋 `1e468150`(형태 보존·[table:ai] 색인 병합·부분 성공 봉투 재사용)·
> `3bb6ff39`($file:N 한 패스·편집 갈린 줄 진단)·`d168b3e4`(IBL 원문 코퍼스)·`a3943c5f`(토큰 원장 초크포인트)
> 가 1판의 여러 항목을 먼저 집행했다. 이 판은 **남은 것**만 적는다. 측정 단계는 두지 않는다(사용자 교정:
> 진단은 09-06 새벽 8턴 실측으로 끝났다 — [[no-counter-watch]]).

## 0. 문제 정식화 (불변)

벽시계 ≈ 출력 토큰 ÷ v, v 는 모델 상수(52~74 tok/s, 턴 종류 무관). 사용자 정식화: "AI 는 언제나 사용자
질문에 대한 최종 보고서를 쓴다" — 보고서 전용이 아닌 모든 턴의 구조. 출력의 행선지는 읽힘(사용자 답) ·
**되받아쓰기**(있는 글자를 다시 침) · **발명**(어제 배관을 다시 지음) · 사고. 줄일 대상은 가운데 둘.

실측(09-06 새벽): 수리 턴 ep2884 29분·129K 토큰·인자 112K자(같은 패치 두 자리·같은 15K자 셸 두 번) /
부동산 ep2890 15.5분·IBL 15.6K자·본문·데이터 22.8K자·`[fn:]` 0 / AI 동향 ep2882 37분·[table:ai] 출력 76% 가 입력 echo.

## 1. 이미 집행된 것 (커밋 기준, 다시 하지 않음)

| 1판 항목 | 상태 | 커밋 | 남은 일 |
|---|---|---|---|
| 2e [table:ai] "새 열만" 계약 | ✅ `_i` 색인 병합, `_merge: index/full` | `1e468150` ② | 04시 주행에서 호출당 출력 토큰 확인 |
| 2d 부분 성공 봉투 재사용 | ✅ `resume_vars{vars_ref,vars,failed_vars}` + `resume:{vars_ref}` 주입 | `1e468150` ③ | **성공 봉투로 확장**(→ §2a) |
| 2d union 형태 비대칭 | ✅ 형태 보존 언어 개정(items 입력엔 items, 빈 items 도 통화) | `1e468150` ① | 없음 |
| 2b `$file:N` 참조 | ✅ 한 패스 치환·범위 밖 거절, 편집 갈린 줄 진단 | `3bb6ff39` | 겹침 관문(→ §2b) |
| 2c 배관 원문 기록 | ✅ `ibl_code_corpus`(원문 해시·성공/실패 누계) 초크포인트 | `d168b3e4` | 되풀이 안내(→ §2c) |
| §1 턴 원장 | ✅ `normalize_usage/record_usage` 초크포인트·관문 | `a3943c5f` | 해부 한 줄은 §4 부산물 |

## 2. 남은 기제 — 되받아쓰기 부류별 "가리키기" 통로

### 2a. 결과 되받아쓰기 → **턴 범위 이름 변수** ✅ 집행(09-06, 사용자 판정)
사용자 질문 "핸들 `$rN` 과 resume_vars 확장 중 장기적으로 무엇이 나은가" → 답은 셋째: **변수 수명을 프로그램 한 번에서
턴으로**. 판정 "그렇게 고쳐" → 같은 세션 집행.
- 왜 `$rN` 이 아닌가: 번호는 봉투 일련번호라 뜻이 없고, 해마 코퍼스에 남으면 그 프로그램은 다른 주행에서 재사용 불가.
- 왜 resume 명시 확장이 아닌가: 참조가 언어 밖(도구 인자)에 살고, 경로 베끼기가 남고, 한 호출에 하나라
  `$a & $b >> [table:join]` 이 안 되고, 표면마다 파라미터 배관이 필요.
- 구현: `backend/ibl/ibl_turn_vars.py`(키=(agent, task_id) → `data/spill/turn_vars_*.json`, 24h GC 동승; code 가 참조하는
  이름만 preset) · 실행기 `_want_live_vars` 컨텍스트 → 내부 키 `_live_vars`(중첩 파이프 봉투는 안 부풀림) · 표면
  `_attach_turn_vars`(단일 step 할당 포함) · 봉투 `turn_vars{injected, live, too_large, note}` · 죽은 마지막 할당의 슬롯 pop
  (오류 봉투가 산 변수로 둔갑하던 자리) · 절단 다음 걸음 문구 · 도구 스키마·MCP docstring·12_ibl_only 한 줄 · ibl.md 개정.
- 규약: 프로그램 안 재할당이 덮는다 · 명시 `resume:{vars_ref}` 가 이긴다 · task_id 없는 직접 표면은 턴 범위 없음 ·
  예약 이름 미주입. `resume` 은 턴을 넘는 24h 회수 자리에 그대로.
- 가드: `backend/test_turn_vars_2026_09_06.py`(다음 호출 참조·세 호출 합성·단일 문장·다른 턴 격리·task 없음·재할당 우선·
  명시 우선·부분 실패 뒤 산 변수·예약 이름).
- ⏳관찰: 다음 정기 주행에서 `turn_vars.injected` 사용 여부와 되받아쓰기 자수.

### 2b. 파일 되받아쓰기 → 겹침 관문 + 줄범위 편집
- `[self:write]`·`[self:edit]` 인자(new_string·content·`$file:N` 해소 뒤) 중 대상 파일 **현재 내용과 그대로
  겹치는** 구간 ≥ 2,000자 = `warning`(줄 a~b 를 짚고 "edit 차분·줄범위·$변수 로 가리킬 것"), ≥ 8,000자 =
  거절. 임계는 `data/lifecycle_policy.yaml` 데이터, 코드엔 상수 이름만. 탐지는 문자열 일치만(어휘 이름 없음).
- `old_string` 되타이핑 오타(끊→끓, ep2884)의 근본은 "모델이 원문을 다시 친다"이다 — 09-05 셸 그림자 관문이
  연 **줄범위 edit**(`edit 줄범위`)를 가이드·거절 처방에서 첫 선택지로 올린다. 갈린 줄 진단(`3bb6ff39`)이
  이미 줄을 짚으므로 처방 문구에 "그 줄범위로 edit" 한 걸음을 잇는다.
- 같은 편집을 두 자리(워크트리+라이브, ep2884)는 수리 경로가 **한 번 적용·기계 전파**: `[self:patch]` propose→
  apply 가 diff 를 옮기는지 확인, 모델이 라이브에 다시 치는 자리를 닫는다(repair_staging).

### 2c. 배관 재타이핑 → 코퍼스가 "이건 이름이 있다"를 말한다
- `ibl_code_corpus` 가 원문 해시·누계를 가진다. 실행 관문에서 들어온 다문장 code 를 슬롯 정규화 해시로
  대조해 **같은 프로그램이 이미 이름(alias)이 있으면** 봉투 `note: "이 프로그램은 [fn:이름]{슬롯…} — 다음부터
  이름으로"`, 이름은 없지만 성공 누계 ≥ 2 면 증류 자동 작명 대상 표시. 실행은 그대로(거절 아님).
- 자동 작명(`code_name→alias`)이 보고서 가지 밖 모든 가지에 적용되는지 확인.
- 05시 주행 `[fn:]` 0 은 가르침만으로 안 먹은 증거 — 위 note 가 관문 한 겹.

### 2d. 같은 실행 두 번 → 봉투가 알린다
- 턴 안 같은 인자 해시 재실행(`rerun_dup`, ep2884 15K자 셸 두 번·ep2882 5쌍)은 두 번째 실행에 `note: "같은
  실행이 이 턴에 있다 — vars_ref 로 주입할 것"`. 실행은 한다(거절 아님).

### 2f. 사고 토큰 — 조치 없음
THINK 게이트는 사용자 판정(장기/위험만) 유지. §2a·2c 가 먹으면 "배관 재발명" 사고가 함께 준다.

## 3. 가르침 — 원칙 한 줄, 판단은 모델
- 12_ibl_only 한 줄 ✅(09-06): **"있는 것은 치지 말고 가리켜라 — 앞 결과는 턴 변수 `$이름`, 디스크는 줄범위 edit·`files_from`, 배관은 `[fn:이름]`."**
- 해마 귀속: 턴 변수 참조(`turn_vars.injected`)·`files_from`·`[fn:]` 을 "가리킴"으로 같이 세어 보상(record_recall_outcome
  옆). 베끼면 귀속, 가리키면 귀속 안 되는 역보상을 남기지 않는다.
- 수리 교리(fragments/13_repair.md) 한 줄: "두 자리에 같은 편집 금지 — 한 번 적용·기계 전파".
- 자기반성 수치 줄(`cognitive_trace.py:579`)에 `되받아쓰기 E자` 추가 — 질문 없음, 수치만(§4 탐지기 부산물).

## 4. 관문·고정물 — 셀 수 있으면 실패시킨다
- 탐지기 `backend/cognition/output_anatomy.py`(층 cognition, `LAYERS` 배정): 순수 함수, 문자열 일치만. 소비처
  셋 = §2b 관문 · §3 반성 줄 · 턴 종료 `[출력해부] typed=… echo=…(result/file/hippo) fn=… dup=…` 한 줄
  (`agent_pipeline.py:763` `[턴비용]` 옆, 이제 원장 숫자가 맞다 — `a3943c5f`).
- 회귀 고정물 `test_output_anatomy_2026_09_06.py`: ep2884·ep2890 로그에서 뜬 인자/결과를 fixture 로, 탐지기가
  기대 echo 구간을 잡는지 + 임계가 policy 한 곳에서만 오는지.
- 주간 감사 `output_anatomy_audit`(doc_drift 옆): 부류별 echo 비율 중앙값 > 20% 면 빨강. 임계는 policy 데이터.
- 모델 교체 시 재보정 불필요(글자 기반).

## 5. 순서·검증

| 순서 | 항목 | 크기 |
|---|---|---|
| ✅ | §2a 턴 범위 이름 변수(집행 09-06) | — |
| 2 | §2b 겹침 관문(탐지기 신설) + 줄범위 처방 + patch 한 번 적용 확인 | 반나절 |
| 3 | §2c 코퍼스 대조 note + 자동 작명 범위 · §2d dup note | 반나절 |
| 4 | §3 가르침 3줄 + 귀속 보상 + 반성 수치 | 2시간 |
| 5 | §4 `[출력해부]`·고정물·주간 감사 | 반나절 |

판정 필요 항목: **없음**(`$rN` 은 사용자 판정으로 턴 범위 변수로 대체·집행, table:ai 계약은 `1e468150` 집행).
검증 = 착수 뒤 정기 주행 3건(04·05·06시)과 다음 #repair 턴의 벽시계·출력 토큰·`[출력해부]` 를 §0 의 09-06
수치와 직접 대조. 기대: 부동산 15.5→7분 안팎, AI 동향 37→12분 안팎, 수리 턴 29→10분 안팎.

**하지 않는 것**: 배관 스위치 자동화 · 티어·기어로 속도 사기 · 어휘 이름을 탐지기에 넣기 · 측정만 심고 두고 보기 ·
새 예약 변수 문법.

관련: [[report-time-is-output-tokens]] · [[report-time-gap-table-ai-cli-oneshot]] · [[ibl-idiom-tier]] ·
[[shell-shadow-gate]] · [[no-counter-watch]] · [[ai-work-not-fixed-program]]

## 6. 되받아쓰기 이음매 전수 감사 (09-06, 사용자 질문 "그런 문제가 더 없는지")

기준: 내용이 IBL 흐름을 타면서 **모델의 출력을 통과**하는 자리(모델이 있는 글자를 다시 침). 근거는 ep2890·ep2884·ep2882 로그와 디스크 산출물.

| # | 이음매 | 증거 | 상태 |
|---|---|---|---|
| 1 | 호출 사이 결과 → 다음 호출 인자 | regions/chungju.json 5,568자: 숫자 155/156·URL 6/6 이 결과 파일에 이미 있음 | ✅ 턴 범위 변수 `d4079418` |
| 2 | **본문 산문 안의 데이터** | 보고서 md 15,291자: 숫자 180/272(66%)·URL 10/20·단지명 7/9 가 결과 JSON 에 있음. 가이드 §2 형식이 "가격/면적/연식·[매물 보기](url)" 행을 손으로 쓰게 함 | ⏳ **가장 큰 잔여** — 데이터 행은 `$items` → document blocks/`보고서HTML` 로 기계가 렌더, 모델은 판정 문장만. 가이드 형식 개정+가리킴 귀속 |
| 3 | GoalEval 평가자 출력 | 프롬프트 "첫 줄 판정+간단 근거"인데 1,898 토큰·37초로 보고서 내용을 재서술(**충족 항목** 인용) | ⏳ evaluate 역할 출력 예산(≈300 토큰)+"미충족 항목만" 형식, 고정물 |
| 4 | 최종 응답 텍스트 | 1,984자가 보고서 머리를 다시 서술(파일에 이미 있음) | ⏳ 정기 보고서 턴의 최종 응답=경로+3줄 규약(가이드) |
| 5 | 의식 초안 → 실행자 재타이핑 | imagined_ibl 524자 vs 첫 호출 497자, 유사도 0.85 — THINK 턴마다 | ⏳ 초안을 턴 변수/선채움 첫 호출로 실행자에게 넘김(agent_pipeline:767 은 관찰만) |
| 6 | `[table:ai]` 입력 echo | 출력 76% 가 입력 되받아쓰기 | ✅ 색인 병합 `1e468150` (04시 확인) |
| 7 | 편집 new_string 의 문맥 줄 | ep2884: old_string 27개 합 199자(앵커는 짧음)인데 new_string 46,613자, 워크트리+라이브 두 번; 낳은 커밋 삽입 +203줄 | ⏳ §2b 겹침 관문(대상 파일과 ≥2,000자 겹침 warning)·한 번 적용 기계 전파 |
| 8 | recall 프로그램 본문 재타이핑 | `[fn:]` 0, 증류 "이름으로 부르지 않음" | ⏳ §2c 코퍼스 대조 note |
| 9 | 절단 → 통째 재실행 | 안내만 있고 통로 없었음 | ✅ resume_vars(`1e468150`)+턴 변수+절단 다음 걸음 문구 |
| 10 | `[self:struct]{grounded}` `_quote` 원문 발췌 | 설계상 echo(환각 방지). 발췌 짧음 | 유지(오프셋은 모델이 못 셈) |
| 11 | 경험증류 code 원문 | 경량 티어, 이번 89 토큰 | 유지(코퍼스 해시 참조로 바꿀 수 있으나 벽시계 무관) |
| 12 | `[others:delegate]{message}` | 이번 주행 0회, `$변수` 참조 가능 | 결함 없음 |

같은 비용 가족이지만 echo 가 아닌 것: 판정 원샷 CLI 새 세션 고정비(4s×38, ep2882) — 왕복 수 문제, 별도.

**우선순위**: ②본문 데이터 렌더(잔여 최대) → ③평가자 출력 예산 → ⑦겹침 관문 → ⑤초안 인계 → ④최종 응답 규약. ⑧은 §2c 그대로.
