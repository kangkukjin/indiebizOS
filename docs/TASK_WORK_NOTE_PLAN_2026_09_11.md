# 결과 전달과 과제 갱신 경계의 수리

상태: **2026-09-12, 3536 분석과 fable 검토를 반영해 A → B·계측 → C로 축소 구현.**
구현 전 기준은 정본 main `5b67e5d6`이다. `dc7195be`의 보완 본문 인계·묶음 패치 예시는
이미 적용됐다. 새 작업노트 저장소·어휘·주입 채널을 만드는 대신 기존 경계만 고쳤다.

## 1. 관측과 해석의 범위

3536은 9월 12일 01:23의 호텔 동 선택 질문이다. 전경 132.6초, 실행 7라운드,
도구 없는 최종 평가 1회로 승인됐다. 보완이 없었으므로 앞선 묶음 패치 수리의 효과를
검증한 사례는 아니다. 3518과 직접 비교해 절감률을 계산하지 않는다.

| 관측 | 이번 수리 |
|---|---|
| 검색·크롤링을 묶어 실행하고 실패 뒤 `$본문` 재사용 | 조합·변수 저장은 이미 작동하므로 유지 |
| 최종 50행·4,430자가 8행으로 표시되고 조회 중 빈 코드 입력도 실패 | A: 기존 미리보기 임계만 6,000자로 조정 |
| 요약의 progress 초과를 수정한 다음 next 초과를 발견 | B: 잘못된 필드를 첫 재요청에 함께 보고 |
| 과제 요약 실패 비용이 마지막 성공 비용에서 빠짐 | B와 함께 기존 후처리 비용 기록 수정 |
| 호텔 동 선택 기준과 옛 경유시간 계산 지시가 함께 전달됨 | C: 장기 규정은 보존하고 현재 실행 지시를 분리 |

요약 모델 호출 3회의 지연 합은 46.2초·44,306토큰이다. 앞선 실패 두 호출만
34.1초·32,758토큰이다. 경험 증류까지 포함하면 후처리 4라운드·약 52.7초 경과다.
기존 `postprocess.json`의 13.51초·15,133토큰은 마지막 성공한 큐 시도만 포함했다.
토큰은 캐시를 포함한 처리량이며 청구액이 아니다. 전경 실행 103.4초에는 도구 시간도 포함된다.

근거는 `data/world_pulse.db`의 episode 3536 / `run_de2db61fe7e4ddb51ce1`,
과제 원장과 [실행 원장](../data/spill/supervision/6b8ac4061ef34216b0f0e336937305f3/events.jsonl)이다.
운영 기록과 과거 비용 파일은 수정하지 않는다.
[전체 반성](SYSTEM_REFLECTION_2026_09_11.md)의 I-1·C-3·C-4 경고에 따라
새 축소 단계·신원 객체·주입 채널은 추가하지 않았다. 이 검토 문서는 별도 미추적 자료다.

## 2. A — 작은 결과는 기존 전달 경계에서 전량 반환

`data/lifecycle_policy.yaml`의 `envelope_preview.min_chars`와 순수 코어 기본값을
3,000 → 6,000자로 바꿨다. 별도 판정이나 업무 예외는 없다. 큰 표의 8행 미리보기,
산문 한도, 원문 참조·변수 보존, 앱 전체 전달은 기존 계약을 따른다.

기존 시험에 50행·약 4KB의 문단을 필터링하고 `provider_tool_result`까지 전달하는
회귀를 추가했다. 한 번에 본문을 받는 것은 확인했지만 실제 모델의 재조회 감소는 아직 측정하지 않았다.
MCP `code` 기본값·도움말·URL 전파는 이번 범위에서 제외했다.

## 3. B — 요약 오류를 함께 보고하고 실패 비용도 누적

`pursuit_bind._validate_answer`가 각 요약 필드를 기존 `pursuit_ledger.validate`로 검증하고
잘못된 필드의 오류를 모아 보고한다. 제한의 정본은 그대로 한 벌이다. JSON 전체를 한 번
재요청하는 기존 경로를 사용하며 부분 필드 재작성·정상 필드 고정 프로토콜은 추가하지 않았다.
절단으로 통과시키지 않고, 전체 검증 뒤에만 원장에 적용한다. 재시도 상한도 그대로다.

`prepare()`는 다음 턴 실행 전에 미완료 요약을 동기로 처리한다. 따라서 배경 실패가
계속되면 다음 전경을 막을 수 있다. 이 경로에서 두 길이 위반을 함께 알리고, 재요청 성공 시
진행하거나 실패 시 원문과 기존 과제를 보존하며 정지하는 것을 검사했다.

`distill_queue._execute`의 `finally`에서 성공·실패 시도 모두 비용을 기록한다.

- 기존 `postprocess.json`: 큐 행 ID·시도 번호로 실행시간·토큰을 누적하며 재개 시 이어 읽는다.
  같은 시도는 중복 가산하지 않는다. 누락 시도나 미확인 토큰은 `incomplete`와 `tokens:null`로 표시한다.
- 기존 `distillation.cost`: 시도별 증분과 성공 여부를 기록한다. 누계 파일과 다시 합산하지 않는다.
- 범위는 후처리 큐의 시도 실행 합이다. 전경의 동기 요약·큐 대기·backoff는 제외한다.
  토큰은 기존 턴 계측의 차이이며 캐시 포함이다. 새 사용량 집계 체계는 만들지 않는다.
- 계측 파일 쓰기 실패가 성공한 기억 쓰기를 재실행하게 하지 않는다. 기존 큐 영속성과 재시도는 유지한다.

실제 첫 요약 성공률과 재요청 감소는 후속 실사용에서 측정한다. 오류를 함께 알려도 모델이
다시 틀릴 수 있으므로 항상 한 번의 수정으로 끝난다고 보장하지 않는다.

## 4. C — 장기 규정을 보존하고 이번 턴 기준을 전달

`run_consciousness`의 keep/amend 경로에서 기존 과제 갱신을 먼저 저장한 뒤,
실행에 반환할 객체의 `imagined_ibl`·`expert_choice`·`capability_focus`를 제거한다.
옛 `approach`도 현재 실행 지시에서 비우고 `task_framing`은 이미 수행한 검토의
`criteria`로 채운다. `guide_files`는 남긴다. 새 모델 판단은 추가하지 않는다.

원장의 장기 `goal_criteria`·framing·접근법·메타는 보존하며 amend의 명시적 규정 수정과
rewrite·사용자 정정 경로도 유지한다. 저장할 규정과 현재 반환 객체를 구분해 이번 턴의
짧은 질문이 장기 목표를 덮어쓰지 않게 했다.

3536 실행자는 낡은 힌트를 무시하고 옳게 조사했다. C의 평가 대상은 호출 수가 아니라
현재 프롬프트의 낡은 지시 제거와 오작동 위험 축소다.

## 5. 검증과 후속 판단

기존 시험 파일에 회귀를 추가했다. 표시·과제·큐 관련 시험은 53개 통과했다.
전체 backend 회귀는 3,935개 통과·1개 건너뜀(254.45초)이다. 기존 amend 시험의
저장 규정=현재 실행 문구 가정도 새 계약에 맞춰 각각의 보존·전달을 확인하도록 고쳤다.
층·파일 크기·IBL 정합성 검사를 통과했고 Android 번들을 재생성했다.
최종 평가는 도구 없이 수행하며 보완은 최대 1회라는 계약을 유지한다.

운영 확인에서는 기존 자식 작업 11개가 남아 자동 재기동의 drain이 끝나지 않았다.
개발 파일 변경이 만든 재기동 요청을 공식 취소 경로로 보류해 기존 백엔드의 요청 접수를
복구했다. 확인 당시 실행 digest는 수정 전 코드였다. 따라서 정본 코드 반영과 실행 중인
백엔드 반영은 구분하며, 이번 수리의 실사용 효과를 관측하려면 정상 재기동이 먼저 필요하다.

고정 재현과 실사용 절감은 구분한다. 후속 에피소드에서 재조회·요약 실패·전경/후처리
모델 라운드와 출력량·지연·품질을 함께 본다. 추가 본문 비용이 이득을 상쇄하면
기존 정책값을 되돌린다. 새 작업노트, 부분 요약 프로토콜, 세션 강제 초기화는 보류한다.

## 6. 수리 후 첫 완료 주행 — 3544 (2026-09-12)

사용자 요청으로 최신 완료 주행을 대조했다. 02:19:20~02:21:11, 호텔 내 매점의 규모와
근처 장보기 장소를 묻는 질문이다. run=`run_218c2cbaacfeaf02a5a8`,
task=`task_d4f417e2`. [실행 원장](../data/spill/supervision/3d42109204b54ccb92878488d21f7348/events.jsonl),
`world_pulse.db:trajectory_event`, 추천 프로젝트의 `pursuit_turn`을 읽기 전용으로 대조했다.

**수리 적용은 확인됐다.** 현재 runtime digest는 정본 코드와 일치하고, 해당 주행의 framing에서
옛 세 메타 키가 빠지고 현재 criteria가 task_framing에 들어갔다. 비용 파일도 새 시도별 형식이다.
앞 절의 재기동 보류는 이 주행 전 해소된 것으로 확인된다. 같은 질문의 직전 3543은 다른
프로세스에서 응답 없이 20.7초 만에 중단됐다(`pursuit_turn.state=interrupted`). 그 기록에는
수리 전 framing과 비용 형식이 남아 있으므로 3544의 수리 후 비용과 합쳐 평가하지 않는다.
중단의 직접 원인은 이번 원장만으로 확정하지 않았다.

| 관측 | 3536 | 3544 |
|---|---:|---:|
| 사용자 응답까지 | 132.6초 | 110.5초 |
| 전경 모델 라운드(분류·선택·검토·실행·평가) | 11 | 11 |
| 실행 모델 라운드 / IBL 호출 | 7 / 6 | 7 / 6 |
| 실행 출력 토큰 | 5,132 | 5,126 |
| 과제 요약 호출 / 모델 지연 합 | 3 / 46.2초 | 1 / 11.0초 |
| 후처리 전체 모델 라운드(요약+경험 증류) | 4 | 2 |

3544는 도구 오류 0건, 최종 평가 1회 승인, 보완 0회다. 서로 다른 질문이므로 시간 차이를
수리의 인과 효과로 보지 않는다. 전경 단축 약 22초 중 평가 경계의 차이가 약 16.2초다
(23.187→7.009초). 실행 출력량과 실행 라운드는 거의 그대로다.

### A — 이번에는 최종 결과 임계가 아니라 중간 결과 표시가 문제

보존된 R1~R4 원문에 3,000/6,000 정책을 비교했다. 3545 후속 감사에서는
`model_result_view.project_result`의 전체 경로(보조 데이터 축소 포함)로도 재확인했다.
네 반환값 모두 같고 현재 정책의 재현은 실제 전달 기록과 일치했다. 외부 도구·모델은 재실행하지 않았다.
즉 이 주행은 6,000자 변경의 실사용 효과를 증명하는 사례가 아니다.

- R2에서 `$마트`를 필터·열 선택·정렬해 **9행·1,044자**를 이미 얻었다. 그러나 뒤에
  이마트 검색 문장이 있어서 이 목록은 중간 결과가 됐고 첫 행만 표시됐다(원장 seq 28, step 4).
- R3에서 같은 목록을 다시 가공하고 `take(9)`까지 붙였지만, 뒤에 경로·영업시간 검색을
  묶어서 또 중간 결과로 접혔다(seq 37, step 5).
- R4에서 같은 목록을 다시 가공해 파일에 저장하고, 별도로 블로그를 크롤링했다.
  R5는 이 파일을 읽는 작업만 수행해 9행 전체를 받았다(seq 52).

`summarize_result`는 중간 items 결과에서 크기와 무관하게 첫 행만 보여준다.
모델은 변수 재사용과 도구 묶음을 했지만, 여러 독립 문장의 필요한 결과가 함께 전달되지 않았다.
R3의 경로·검색과 R4의 크롤링은 새로운 정보를 얻으므로 두 라운드 전체를 낭비로 세면 안 된다.
우선 절감 후보는 R5의 파일 재조회 1라운드와 R3·R4의 반복 가공 코드다. 모든 중간 데이터를
펼치기보다 필요한 완료 결과를 함께 반환하는 기존 조합·반환 계약부터 검토할 근거다.

### B·계측 — 첫 성공은 관측됐지만 오류 집계 분기는 발동하지 않음

요약은 1회에 성공했고 과제 턴도 `applied`다. 이어 경험 증류 1회가 실행됐다.
두 호출의 입력+출력 합 **14,248토큰**이 `postprocess.json`과 일치한다
(`queue_id=266`, attempts=1, elapsed_s=12.748, incomplete=false, succeeded=true).
이번에 실패가 없었으므로 오류를 함께 알리는 새 분기와 실패 비용 누적 자체는 실주행으로
검증되지 않았다. 3→1회를 전부 B의 효과로 주장할 수 없다.

### C — 옛 실행 초안은 제거됐고 과거 전제는 남음

현재 framing은 매점·장보기 질문이고 imagined_ibl·expert_choice·capability_focus는 없다.
하지만 assumptions에는 옛 송어횟집 이름을 경로 도구가 해석할 수 있다는 전제가 남았다.
그 전제에 대한 추가 조회는 없었으므로 관측된 비용은 프롬프트 잡음이다. 특정 단어 삭제 규칙을
추가할 근거가 아니라 장기 전제와 이번 턴에 검증할 전제의 전달 의미를 점검할 근거로 남긴다.

응답 품질도 평가 승인과 별개로 본다. 정확한 영업시간을 못 읽었다는 고지는 있었지만,
과거 후기의 휴무 정보로 미래 특정일의 영업을 단정한 표현은 근거보다 강하다.
추가 모델 호출을 늘리기 전에 기존 평가가 이런 확정 어조를 다루는지 확인할 부분이다.

## 7. 다음 주행 — 3545, 오송 주인세대 전세 검색 (2026-09-12)

02:25:54~02:28:47, 부동산 에이전트. run=`run_e24d78b0859633266bed`, task=`task_5693803f`.
[실행 원장](../data/spill/supervision/527c0a30957141bcbfb78bdeaa571314/events.jsonl)과
`world_pulse.db:trajectory_event`, 부동산 프로젝트의 과제 원장을 읽기 전용으로 대조했다.
코드는 `308bbaa7` 수리 뒤 그대로다. 외부 매물이나 모델을 다시 조회하지 않았다.

| 단계 | 모델 라운드 | 경과 |
|---|---:|---:|
| 분류 | 1 | 0.5초 |
| 사전 의식: realty 계약 조회 후 계획 작성 | 2 | 78.3초 |
| 실행: 기억 조회·매물 조회 3회·네이버 펼치기·직방 재조회·답변 | 5 | 70.1초 |
| 최종 평가 | 1 | 23.2초 |
| 전경 전체 | 9 | 172.3초 |

실행의 IBL 호출은 4회, 내부 도구 실행 합은 1.342초(실행 경과에 포함), 오류 0건이다.
계획 출력은 4,889토큰으로 실행 출력 4,165토큰보다 많다. 계획이 전경 시간의 약 45%이며
계획·평가를 합치면 약 59%다. 계획에서 현재 의미와 소스 선택을 판단할 필요는 있으나,
단순 조회의 계획을 framing·전문가 선택·기준·힌트·초안으로 반복 작성하는 비용은 별도 절감 후보다.
계획 전체를 없애도 품질이 같거나 78초가 그대로 절약된다고 입증한 것은 아니다.

### A의 실제 전달 효과 확인

R3은 `$다가구`의 title/meta/summary/url만 선택했다. 원문은 지도 데이터까지 6,988자이며,
실제 `project_result`는 먼저 지도 보조 데이터 3,247자를 접어 **17행·3,792자**로 만든다.
이 순서를 빼고 `diet_envelope → preview_envelope`만 재현하면 두 임계 모두 8행이라고
잘못 판정하므로 실제 진입점 전체를 사용했다. 증거 저장·사건 기록만 메모리 대역으로 바꿨다.

- 3,000자 정책: 17행 중 8행 전달.
- 6,000자 정책: 17행 전량 전달. 재현 출력은 실제 R3 전달 기록과 일치.
- 추가로 보인 9·10행에 `2647814738`·`2647262618` 주인세대가 있고, 실제 최종 답변에
  둘 다 포함됐다. 따라서 추가 회수 없이 후보 두 건을 볼 수 있게 한 전달 효과는 확인됐다.
  과거 정책에서도 모델이 다시 펼쳐 찾았을 가능성은 있으므로 실주행 라운드 절감량은 미확정이다.

### 재조회 두 종류를 구분

R2는 네이버 단독·다가구 17건, 네이버 빌라 0건, 직방 1건을 한 호출로 받았다.
앞의 네이버 결과는 첫 행 요약만 보여 R3에서 `$다가구`를 다시 펼쳤다. 이는 3544의
마트 목록과 같은 중간 결과 전달 문제다. 여러 업무에서 반복된 반환 계약의 문제로 볼 근거가 늘었다.

직방은 R2의 최종 결과라 1건 전체를 이미 전달했다. 그런데 R4는 `$직방`에서
title/summary/address/url을 다시 뽑았다. 네 필드를 대조하니 **모두 R2 값과 동일**했다.
새 정보·갱신·추가 검증이 없는 재조회이므로, 현재 흐름에서 가장 명확한 절감 후보는 R4 1라운드다.
다만 이 원장만으로 모델이 이미 받은 정보를 다시 읽기로 한 내적 이유까지 확정하지 않는다.

### B·C 적용 범위와 후처리

의식은 `scope:turn`으로 새 계획을 작성했다. 부동산 프로젝트의 pursuit와 pursuit_turn은
모두 0행이며 선택·검토·과제 요약 호출도 없다. 따라서 이번 새 초안·힌트는 낡은 규정의
재유입이 아니고, 이번 주행은 B의 요약 검증이나 C의 keep/amend 수리를 검증하는 사례도 아니다.

후처리는 경험 증류 등 3모델 라운드, 실패 0건, 큐 1회 성공이다.
입력+출력 합 8,032토큰이 비용 파일과 맞는다(attempts=1, elapsed_s=10.626,
incomplete=false, succeeded=true). 전경 시간과 별도로 본다.

최종 평가는 한 번에 승인됐고 링크는 수신 자료에서 확인된다. 다만 '방3·화장실2·최상층 전체
조건에 맞는 것' 표에 들어간 직방 후보는 수신 자료에 화장실 수가 없다. 확인된 조건과
미확인 조건을 구분하는 품질 문제는 남는다. 매물 자체의 현재 유효성은 이번 감사에서 재조회하지 않았다.
