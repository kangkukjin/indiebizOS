# 세계 지도 — 어휘만으로 지식을 떠올리게 하기

2026-09-17. 기준 구현 `4c53faa8`.

## 변경

AI가 이름에서 이미 회상할 수 있는 지식까지 매번 설명하던 주입 형식을 줄였다.
검색·상세 조회용 카탈로그와 모델에게 전달하는 조각은 다른 표현이다.

- 자동 주입: 이름과 관계. 같은 주어의 관계를 한 줄로 묶는다.
- 연결 없는 어휘: 분류와 이름. 동명이의어는 분류로, 분류도 같으면 ID로 구별한다.
- 명시 적용 범위·적합/부적합 관계는 보존한다. 충족 여부를 새로 판단하지 않는다.
- 뜻·긴 설명·반복 행동 지침·조회 명령·해시는 자동 주입에서 제외한다.
- 설명·출처·조건 판단 상태·revision/digest·생략 기록은 내부 문맥과 사건 또는 상세 조회에 남긴다.
- names 모드도 분류와 이름만 전달한다. 공통 주입 대상과 권한 경계는 이전과 같다.

실제 기본 설정으로 같은 주입 함수를 호출해 비교했다. 문자 수는 태그를 포함한다.

| 입력 | 이전 | 현재 |
|---|---:|---:|
| Blender | 1,208 | 140 |
| 집을 3차원 렌더링으로 그려보고 싶어 | 1,596 | 200 |
| 직원 근무표를 짜줘 | 1,300 | 176 |

Blender의 실제 주입 전체:

```text
<method_map>
세계 지도 · 참고 어휘
<world_data>
Blender: 구현=장면 기반 렌더링
장면 기반 렌더링: 필요=렌더 장면 입력; 상위=3차원 장면 표현; 용도=건축 공간 시각화
</world_data>
</method_map>
```

## 검증

- 지도 검색·구조·탐색과 공통 파이프라인: 116 passed.
- 고정 과제 검색/문맥 검사: 16/16. 이는 모델 응답 품질 검사가 아니다.
- 이름의 구별, 관계 방향, 명시 조건과 부적합 의미, XML 이스케이프, 내부 근거 보존을 검사했다.
- 마지막 동명이의어 표기 보완 후 구조·문서 검사: 42 passed.
- 전체 백엔드 회귀: **4,978 passed, 1 skipped**, 498.31초.
  로그 `/tmp/world-map-full-native.log`, JUnit `/tmp/world-map-full-native.xml`.
- 카탈로그·IBL 파생 정합 검사와 폰 번들 매니페스트 재생성을 완료했다.

설명 축소의 실제 모델 품질 효과는 별도 비교 대상이다. 이번 검사는 주입 길이와 구조 전달을 확인한다.
