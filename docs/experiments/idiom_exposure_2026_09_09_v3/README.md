# 관용구 노출 재실험 v3 (2026-09-09, 지렛대 1~4 뒤)

보고서: [IBL_IDIOM_EXPOSURE_LEVERS_2026_09_09.md](../../IBL_IDIOM_EXPOSURE_LEVERS_2026_09_09.md). 기준 코드 `aa5bd220` + 워킹 트리(지렛대 집행분, 같은 커밋에 포함).

- `manifest.json` 계획·스케줄·프롬프트 해시·소스 해시 · `registry.json` 두 조건에 공통으로 등록한 세 몸 스냅샷
- `validation.json` 기준 풀이 18·오답 3 사전 검증 · `run.log` 실행 순서 · `trial_metrics.csv`/`summary.json` 집계
- `compact_trials.json` 54회 × 시도별 코드·판정·fn 호출·잎 호출·사용량 · `exposure_delta.txt` 노출 조건에만 있던 22줄(병기 3줄 + 부록) · `prompt_sha256.json`
- 원본 프롬프트·CLI 응답 전문: 로컬 `outputs/idiom_exposure_2026_09_09_v3/`
