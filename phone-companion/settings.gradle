pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.PREFER_SETTINGS)
    repositories {
        google()
        mavenCentral()
    }
}
rootProject.name = "PhoneAgent"
// memberOnly 빌드는 기존 앱의 Chaquopy/BaseBundle 구성까지 제외한다.
if (!gradle.startParameter.projectProperties.containsKey('memberOnly')) include ':app'
include ':member'
